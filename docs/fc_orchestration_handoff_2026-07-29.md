---
doc_type: handoff
project: suridoksuri-1
scope: 2026-07-29 FC 오케스트레이션 세션 — 인수인계
last_updated: 2026-07-29
---

# 2026-07-29 FC 오케스트레이션 인수인계

> **이 문서 하나로 이어받을 수 있게 썼다.** 상세 근거는 각 절이 가리키는 문서에 있다.
> 트랙 보드(`docs/session_status.md`)도 갱신돼 있으나 **결정 대기 항목은 여기가 정본**이다.

---

## 0. 🔴 먼저 — 사용자 결정 대기 2건 (D-1·D-3 해소)

이어받는 세션은 **이것부터 확인하고 시작**한다. 둘 다 사용자만 답할 수 있다.

| # | 사안 | 상태 |
|---|---|---|
| ~~D-1~~ | ~~`transition_alt` 50 → 25 m~~ | ✅ **해소 (`fc8631d`, 실기체 배포 완료).** `waypoints` 도 150 m 편도·z=25 로, `vision_search_timeout` 120→180 s. §3 참조 |
| ~~D-3~~ | ~~재탐색 반경 `vision_retry_radius`~~ | ✅ **해소 (2026-07-29 사용자 답변: "더 낮게 유지 — 구현이 맞음").** 원안이 "더 넓게"가 아니었음이 확인됐다. **코드 변경 없음.** 1회차 39.4 m 안에서 못 찾으면 재탐색으로도 못 찾는다는 한계는 **의도적으로 수용**한 것이다 — 다시 제안하지 말 것 |
| **D-5** | 🔴 **`range_limit_m` 300 m 고정 해제 — 경로 비례 자동 산출** | **사용자 지시. 미구현(코드 변경 필요) — §7 에 설계와 근거.** ⚠️ **긴급도 하향:** 2026-07-29 스윕 실측으로 편도 25~150 m 전 구간에서 실제 최대 이탈이 **102.5~188.0 m** 라 300 m 가 안 걸린다(§9). 구조 문제는 그대로이나 **당장의 비행 블로커는 아니다** |
| **D-2** | **`a_max_g` 0.3 → 0.43** | 🔴 **D-4 와 한 몸이다 — 따로 결정하면 안 된다.** 손익분기 **0.41 → 0.47** 은 **TECS 처방을 넣었을 때** 값이다. D-4 를 실기체에 적용하지 않으면 분기는 **0.41 그대로**이고 **0.43 은 초과**한다 |
| **D-4** | **TECS 파라미터 실기체 적용 여부** | SITL 권고값 `FW_T_SPDWEIGHT 0.5` + `FW_T_ALT_TC 3.0`(회수 −31 %). **SITL≠실기체 파라미터라 그대로 옮기면 안 된다** — §5 주의 참조. 실기체 전용 재도출이 선행돼야 한다 |

**사용자가 지정한 다음 급소는 F-6(코너 불연속)이다** (2026-07-29 확인). §2-3 참조.

---

## 1. 오늘 완료된 것 (전부 실기체 배포·검증 완료)

| 항목 | 결과 | 커밋 |
|---|---|---|
| **PX4 재플래시 + CRSF 사고** | 1차 플래시가 ELRS 조종기를 두절시킴(순정 config 에 `crsf_rc` 없음) → 재빌드·재플래시·검증 | `0f1583b` `f2d8c12` |
| **F-7 헤딩 정렬 마진** | `wp0_heading_tol` 0.05(2.9°) → **0.2618(15°)** | `58ca4a7` |
| **F-9 천이 고도계단** | `fw_setpoint_alt()` 램프. C1a +29.3→**+7.6 m**, C1b −70.6→**−8.3 m** | `94989b6` `f6f8789` |
| **F-10 종점 포착(C1b)** | 거리 원 → **결승선**(수직 반평면) + `following_timeout` 240 s. C1b 21.4 s 로 결정론화, 최저 AGL 33.5→**79~82 m** | `8bbcb94` `a330eb9` |
| **F-12 플래너 블로킹** | NR 3중 낭비 제거. L자 26.1→**0.098 s**, 폐회로 99.4→**0.305 s**, 경로 불변 | `15f7fa4` |
| **F2 블로커 2건** | `_enter_vision_search` 가 `_sm` 미세팅(= `vision_landing:=true` 가 통째로 죽어 있었다) + 래치가 제어틱을 셈 | `9c5d17f` |
| **F2 후속 6건 + F2-g** | F2-g: `_pl_alt` 하한 == `handoff_agl`(3.0 m)이라 **고도 기반 인계가 한 번도 실행된 적 없음** | `db7aa9c` `3d73bfb` |
| **F-5 원인 규명 + TECS 검증** | 침하의 86 % 가 고도↔속도 배분 오류. 처방 회수 **15~50 %** | `b3e35c7` `db4bb8c` |
| **안 C 1단계** | G2 스파이럴 프로토타입. `\|κ\|max` 161→**1.25~1.42**(이론하한 1.13~1.26). **2단계는 보류** | `afce94d` |
| **배터리** | 조사 결과 Emergency 는 **오탐**(만충 팩). 사용자 결정으로 **종결·무시** | `01d1275` |
| **운용 고도·경로·타임아웃** | `transition_alt` 50→**25 m**, `waypoints` 300→**150 m 편도**(z=25), `vision_search_timeout` 120→**180 s**. 테스트 3건이 낡은 값을 고정하고 있어 일반화 | `fc8631d` |
| 인프라 | 하니스 전역 `pkill` → 자기 프로세스 그룹 한정 / 테스트 기준선 **783건** 명문화 | `bc3229e` `51b173e` |

**테스트 기준선 783건**(`fc_bridge/tests` 260 + `fc_ros` 394 + `vtol_sim` 47 + **`tools` 82**).
세는 법은 `CLAUDE.md` 공통 규칙에 박아뒀다 — `tools` 누락과 stale `__pycache__` 로
하루에 다섯 가지 숫자가 보고된 이력이 있다.

---

## 2. 미해결 — 다음에 할 것

### 2-1. 🔴 물리 2건 (사용자 작업, 코드로 못 푼다)

**`docs/preflight_physical_checklist.md` 가 현장 실행용 정본이다.**
사용자가 "비행전 물리문제 해결을 위한 체크리스트"라고 하면 그 문서를 연다.

| | 내용 | 합격선 |
|---|---|---|
| **pusher** | FC 지령·PWM·프롭 기계는 **전부 정상 확인**. 남은 건 ESC·모터·3상배선·ESC 아밍 | 프롭 제거 후 통전 → **회전 여부**로 갈린다(계측기 불필요) |
| **자기계** | `\|B\|` 가 기수에 따라 **−10 %**(북동 0.4213 → 남서 0.3792 G). `EKF2_MAG_CHK_STR` 하한 80 % 기준 남서 **75.4 % 탈락** | 야외 GPS fix 후 **위치 고정** 4방위, **남쪽에서 `test_ratio < 1`** |

실내 측정은 한계에 도달했다(GPS 없어 `ref=nan` → 채점 자체가 안 됨). 근거는
`docs/fc_ground_diagnostics_2026-07-29.md`.

### 2-2. F2 SITL 실증 — **c·f·g 해소, d 만 남음** (2026-07-29 갱신)

| | 상태 |
|---|---|
| **F2-c**(생산자 사망) | ✅ 실증 — `V5b`. `link ERROR → GPS 착륙 폴백` 이 **13 s** 에 발화(시한 60 s) |
| **F2-f**(align 타임아웃) | ✅ 실증 — `V6`. 함정대로 `v_approach=0.5` + `hold_timeout=20` 으로 재현했다(`wp1_land_radius` 가 launch 인자 **미선언**) |
| **F2-g**(인계 고도) | ✅ 실증 — 전 런. `인계고도 도달 (3.5m AGL, 목표 3.0m±0.5)` |
| **F2-d**(유도 상실) | 🔴 **남음.** `--linkdead-at` 이 아니라 **`--exit-at`** 을 써야 한다 — shim 자신이 죽으면 `link_dead` 가 안 서고 **`vision_lost_timeout=5.0` 이 메워야** 정상이다. **그 경로는 아직 한 번도 실행된 적이 없다.** 명령은 `f2c_v7.sh` |
| `vision_landing=false` 종점 완주 | 🔴 전용 런 미실시(단 종점 도달 자체는 6런 중 5런에서 관측) |

근거·재현 명령은 `logs/2026-07-29_f2_followup/notes.md`.

### 2-3. R5/R6 잔여

- **F-8**(`_cruise_alt` 스칼라화) · **F-11**(짧은경로 `_FW_LOOKAHEAD`)
- **F-6 코너 불연속** — lookahead 목표점이 **한 틱에 50.5 m 점프**해 코스 지령이 20 ms 에 48° 계단이 되고
  롤이 `FW_R_LIM` 까지 간다. **F-5 와 F-6 은 코너에서 같은 사건**이고,
  TECS 검증이 "침하를 근본적으로 줄이려면 뱅크를 낮춰야 한다"로 수렴했으므로 **F-6 이 다음 급소다.**
- **F-13** 미사용 속도 프로파일 — 오프보드에서 `cruising_speed = NAN; // ignored` 하드코딩이라
  플래너가 `v_ref` 를 내도 하류에서 소비되지 않는다(F-5 세션 확인).
- 미규명: MC HOLD 수직가속 상승(A1 5.98→10.34, C2 6.71→11.73 m/s², B8 불변)

### 2-4. 안 C 2단계 (보류)

프로토타입은 `--planner g2spiral` 로 수동 선택 가능. 교체·SITL 전면 재검증은 **사용자 보류 결정**.
전장이 8~12.5 % 늘어 타임아웃·배터리 예산 재확인이 선행돼야 한다.

---

## 3. ✅ `transition_alt` 50 → 25 m — 해소됨 (`fc8631d`)

**사용자:** "yaml 상 default 상승고도는 너무 높다. 실제 대회에서도 끽해야 20 m 올라가고,
무슨 일이 있더라도 30 m 가 의도적으로 올라가는 최대고도일 것이다."

```yaml
transition_alt:  50.0
waypoints: [0.0, 0.0, 50.0, 300.0, 0.0, 50.0]     # z 도 50
```

**오늘 터진 결함 3건이 전부 이 값에 증폭됐다:**

| 사건 | 고도가 어떻게 작용했나 |
|---|---|
| F2 래치 실패 | HOLD 고도 49.6 m 에서 래치 → 하강만 `(49.6−3.0)/0.8 = 58.3 s` → **60 s 타임아웃 초과** → GPS 폴백 |
| flight02 F-9 | `transition_alt` 유실로 50 m 상승, WP z=21.2 → **−29.6 m 계단 최대폭** |
| C1b 접지 | `transition_alt=120 m` 시험 → 70 m 고도차로 2바퀴 → 최저 AGL 이 깎여 접지 |

탐색 고도가 25 m 인데 HOLD 가 50 m 라 **25 m 를 내려간 뒤에야 탐색이 시작**되고,
검출 상한(33.57 m)도 그 사이에 있다.

**그리고 이제 낮출 수 있다** — F-9 수정으로 천이 고도계단이 −29.6 → −8.3 m 로 줄었다.
종전 50 m 기준으로는 20 m 천이가 위험했으나(계단만으로 지면 근처) 지금은 여유가 생겼다.

### 적용 결과 (`fc8631d`, 실기체 배포·md5 검증 완료)

| | 종전 | 현재 |
|---|---|---|
| `transition_alt` | 50.0 | **25.0** |
| `waypoints`(offboard/mission, yaml + 노드 declare) | 300 m 편도, z=50 | **150 m 편도, z=25** |
| `vision_search_timeout` | 120 s | **180 s** |

**`waypoints` 의 z 를 `transition_alt` 와 같은 25 로 맞춰 F-9 계단이 0 이 된다.**

테스트 3건이 낡은 값을 계약으로 고정하고 있어 함께 일반화했다(**F2-g 와 같은 실패 모드 —
테스트가 결함을 보호한다**):
`test_waypoints_altitude_50m` → `..._matches_transition_alt`(절대값이 아니라 **관계**를 보고,
대회 상한 30 m 도 단언) · `test_new_params_present_in_yaml`(값 고정 → 존재 검사) ·
`test_r1_climbing_timeout_...`(`predicted == approx(27.5)` 하드코딩 제거, **여유 배수만 계약**).

⚠️ **실제 미션 WP 는 여전히 갱신 대상이다** — 150 m 편도는 어디까지나 기본값이다.

---

## 4. 탐색 비행 — 사용자 원안과의 대조

| 사용자 원안 | 구현 | |
|---|---|---|
| 마지막 WP 도달 후 시작 | `FOLLOWING → TRANSITION_MC → HOLD → VISION_SEARCH` | ✅ |
| MC 모드 | HOLD 이후라 MC | ✅ |
| 점점 원을 크게 | 나선 **6.1 → 30 m**(링 간격은 풋프린트에서 자동, 겹침 35 %) | ✅ |
| 못 찾으면 고도 낮춰 1회 더 | 2회차 **25 → 15 m**, 최대 2회 | ✅ |
| — | **반경도 30 → 18 m 로 줄인다** | ✅ **D-3 해소 — 사용자 확인 "구현이 맞음"**(§0) |

시간 예산: 1회차 나선 89.4 s + 재탐색 65.7 s = 155.1 s, **최악 총계 171.1 s**
(회차당 `vision_search_timeout` **180 s** — 종전 120 s 는 1회차 예산 122.4 s 를 이미 초과했다).
**`transition_alt` 가 25 m 가 되면서 HOLD→탐색고도 하강 자체가 사라졌다.**

---

## 5. TECS 처방 결과 (D-4 근거)

28 런(유효 24), B2·B3·B4·B5 × 8 설정. **코드 변경 0, PX4 파라미터만.**

| 설정 | 침하계수 회수 | 침하(B4) | 동적 속도여유 |
|---|---|---|---|
| 1.0/5.0 (현행) | — | 7.56 m | 1.13~1.35 |
| 0.5/5.0 | −15 % | 6.70 | 1.05 |
| 1.0/3.0 | −19 % | 6.05 | 0.94 |
| **0.5/3.0 (권고)** | **−31 %** | **5.22** | 0.39~1.16 |
| 0.0/3.0 | −50 % | 3.83 | 0.81 |

- **1·2순위 우열 없음** — 서로 다른 자리(초기응답 vs 최종지령)에 작용해 **더해진다.**
- **원 보고서 §8 의 "76~86 % 회수" 는 과대평가.** 실제 15~50 %.
- **근본 원인:** 수직력 결손은 0.07~0.09 로 **붙박이**이고 줄어드는 건 **지속시간**(3.60→2.61 s).
  실효 받음각이 모델 실속각(19.43°)을 넘어간다 ⇒ **침하를 근본적으로 줄이려면 뱅크를 낮춰야 한다(F-6).**
- 속도 판정선 정정: 정적 `FW_AIRSPD_MIN`(10)이 아니라 **뱅크 보정 `10·√(1/cosφ)`**(55°에서 12.5).
- `ALT_TC = 2.0` **기각**(상방 오버슈트 +3.28 m).

⚠️ **SITL ≠ 실기체:** `FW_T_CLMB_MAX`(8 vs 5)·`SINK_MAX`(2.7 vs 5)·`P_LIM_MIN`(−15 vs −30)·
`PSP_OFF`(2.0 vs 0.0)·`THR_MAX/TRIM`(0.6/0.25 vs 1.0/0.6)·`NPFG_PERIOD`(12 vs 10).
**실기체 파라미터를 SITL 기체에 얹는 시도는 실패했다** — `FW_THR_MAX=1.0` 에서
TAS 39 m/s·뱅크 88.7°·제어 상실. 역방향도 그대로 옮기면 안 된다.

---

## 6. 이어받을 세션이 반드시 알아야 할 함정

1. **SITL 은 단일 테넌트다.** px4/mavros 포트 14540/14580 고정 → 런이 겹치면 **양쪽 다 죽는다.**
   **SITL 세션은 동시에 1개만** 굴린다. (오늘 병렬 3개를 굴려 런 7건 이상을 날렸다.)
2. **`fc_bridge` 는 md5 대조로 stale 을 못 잡는다.** `colcon build` 는 `fc_ros` 만 `install/` 로
   옮기고 `fc_bridge` 는 **`PYTHONPATH` 로 로드**된다(기본 셸에서 `import fc_bridge` 실패).
   검증 명령은 `preflight_physical_checklist.md` §5-b.
3. **단일 런 A/B 는 성립하지 않는다** — 같은 설정 기준선 4런이 7.07/8.31/7.49/7.39 m(폭 16 %).
4. **`PX4_DIR=/root/PX4-vehicle` 를 반드시 명시**하고 `make px4_sitl_default` 를 재실행한다
   (기본값은 다른 취약 빌드, stale 바이너리 실측 이력).
5. **stale `__pycache__` 가 테스트 7건을 거짓 실패시킨 사례**가 있다. 파괴검증 시 매 전환마다 제거.
6. **노드 `range_limit_m` 기본 300 m** 가 B3(354 m)·B2(492 m) 코너를 OVERRIDE 로 끊는다.
   캠페인엔 없던 가드(`ca8809d`)이므로 아카이브와 비교할 땐 800 m 로 통일한다.
7. **배터리는 종결된 사안이다.** `COM_LOW_BAT_ACT` 를 켜면 만충 팩으로도 이륙 7초에 자동 착륙한다
   (`docs/fc_battery_gate_survey.md` 최상단 배너). 다시 제안하지 말 것.
8. **세션 지시문에 "안전장치·가드로 보이는 것을 제거할 때는 실행 전 보고"를 넣을 것.**
   오늘 한 세션이 격리 클론에서 로컬 가드를 자체 판단으로 제거해 보안 경고가 떴다
   (결과적으로 무해했으나 판단 자체를 세션에 맡기면 안 되는 종류다).

---

---

## 7. 🔴 D-5 — 지오펜스(`range_limit_m`) 를 300 m 고정에서 경로 비례로

### 7-1. 사용자 지시와 그 근거

> "지오팬스를 300m로 고정하지 마라. 기존 300m 지오팬스의 의미는 **'아무리 생각해봐도 정상
> 거동에서는 나올 수 없는 크기의 위치벡터는 지오팬스로 차단'** 이다. 200m 편도 직선 경로에서
> 마지막 wp에 역천이 드리프트로 50m 밀리고, 거기서부터 원중심 잡고 탐색하면 걍 지오팬스에 걸리는거다."

**이 판단은 실측으로 이미 뒷받침돼 있었다.** F2 검증 세션이 다른 시나리오에서
`range_guard.max_horiz_m` **331~344 m**(역천이 오버슈트분)를 관측했고, 별도로 **F2-e**
("`_check_path_within_range` 가 탐색 반경을 안 더한다")로도 걸렸다. 즉 300 m 고정은
**이미 정상 거동에서 걸리고 있었다.**

### 7-2. 정량

필요 반경 = `경로 최대 이탈` + `역천이 오버슈트` + `탐색 반경`

| 경로 | 오버슈트 44 m(실측 하단) | 오버슈트 60 m(보수) | 300 m 고정으로 |
|---|---|---|---|
| 150 m (현 기본값) | 224 m | 240 m | 통과 (여유 60 m) |
| 200 m | 274 m | **290 m** | **여유 10 m — 사용자 지적 그대로** |
| 300 m (종전 기본값) | 374 m | 390 m | **걸린다** |

### 7-3. 설계안 (미구현)

다른 파라미터의 **`0 이하 = 자동`** 규약(`vision_search_spacing`·`vision_latch_max_agl`·
R1 타임아웃 4종)을 그대로 따른다.

```yaml
# 0 이하면 자동 산출: path_max_range(_pts) + 역천이 오버슈트 마진 + vision_search_radius + 안전여유
range_limit_m: 0.0
```

- **`path_max_range(_pts)`** — 이미 있는 함수. 편도·폐회로를 모두 올바로 다룬다
  (이륙지점 기준 최대 거리이므로 폐회로에서 과대평가하지 않는다).
- **역천이 오버슈트 마진** — 실측 30~44 m. **60 m 권고**(바람 여유 포함).
- **`vision_search_radius`** — F2-e 가 지적한 누락분. `vision_landing` 이 꺼져 있으면 0.
- **안전여유** — 50 m 권고.

⇒ 150 m 경로면 290 m, 200 m 면 340 m, 300 m 면 440 m. **경로에 비례하면서도
"정상 거동에서 나올 수 없는 크기"라는 원래 의미가 유지된다.**

### 7-4. 오케스트레이터 의견 (사용자가 요청)

**사용자 판단이 맞고, 보탤 것은 두 가지다.**

1. **고정값을 "크게" 잡는 것으로는 해결되지 않는다.** 400 m 로 올리면 150 m 경로에서는
   지오펜스가 사실상 무의미해진다(정상 거동의 2.7배까지 허용). 이 가드의 값어치는
   **"정상보다 조금 큰 값"** 에서 나오므로 **경로에 비례해야만 의미가 유지된다.**
2. **`vision_landing` 이 켜지면 필요 반경이 탐색 반경만큼 더 커진다.** 지금은 그 항이
   빠져 있어(F2-e) `true` 로 날리는 순간 여유가 30 m 줄어든다. 자동식에 반드시 포함할 것.

**⚠ 구현 시 주의:** `RangeGuard`(감시 스레드)와 `_check_path_within_range`(이륙 전 경고)가
**같은 값을 봐야 한다.** 한쪽만 고치면 "경고는 안 나는데 비행 중 잘린다"가 된다.

---

## 8. 타임아웃 — 일부 반영, 나머지 재검토 필요

사용자: **"타임아웃이 너무 빠듯하다."**

### 8-1. 반영됨 (`fc8631d`)

| | 종전 | 현재 | 근거 |
|---|---|---|---|
| `vision_search_timeout` | 120 s | **180 s** | 1회차 실측 예산이 나선 89.4 + 정렬 30.0 + dwell 3.0 = **122.4 s** 로 **이미 초과**했다 |

### 8-2. `transition_alt` 25 m 로 자동 완화된 것

| | 25 m 에서 | 비고 |
|---|---|---|
| CLIMBING 체류 | 27.5 → **18.7 s** | `climbing_timeout` 120 s 대비 6.4배 여유 |
| F2 하강(래치→3 m) | 58.2 → **27.5 s** | `precision_land_timeout` 60 s 대비 여유 |
| HOLD→탐색고도 하강 | **불필요** | 탐색고도가 25 m 라 이미 그 자리다 |

### 8-3. 아직 검토 안 한 것 (다음 세션)

- **`hold_timeout` 30 s** — WP1 반경 3.0 m 안에 들어와야 하는데 **역천이 오버슈트가 30~44 m** 다.
  `v_approach` 5 m/s 로 복귀에만 6~9 s + 정착. 바람이 있으면 빠듯할 수 있다.
  ⚠️ HOLD 가 타임아웃으로 끝나면 **F2 align 도 같은 조건(`dist < wp1_land_radius`)에 막힌다**
  (F2-f 로 완화했으나 근본은 남았다).
- **`landing_timeout` 60 s** · **`mc_wp_timeout` 20 s** — 실측 대조 미수행.
- **회차당 180 s × 2 + 정렬/dwell** 이 대회 임무 시간 안에 드는지 확인 필요.

---

## 9. 2026-07-29 실기체 직전 검증 — 짧은 경로 스윕 (신설)

**현장 절차 정본은 `docs/field_vision_landing_2026-07-29.md` 다.** 여기는 결론만 남긴다.

편도만 바꿔가며 `vision_landing=true transition_alt=25.0`, `range_limit_m` 기본 300:

| 편도 | FOLLOWING 체류 | 오버슈트 | HOLD 복귀 | 래치 AGL | 총 수평 이탈 | 결과 |
|---|---|---|---|---|---|---|
| 150 m | 8.2 s | 48.1 m | 12.6 s | 24.8 m | 188.0 m | 통과 |
| 60 m | 2.4 s | 49.3 m | 12.6 s | 24.9 m | 112.6 m | ✅ 완주 |
| 40 m | 1.0 s | 68.3 m | 17.0 s | 24.9 m | 111.8 m (횡 −17.2 m) | ✅ 완주 |
| 25 m | **0.2 s** | 73.2 m | 17.6 s | 25.0 m | **102.5 m** | ✅ 완주 |

### 9-1. 🔴 **F-11 은 실제로 일어나지만 임무를 깨지 않는다**

FOLLOWING 창은 예고대로 소멸했다(**8.2 → 0.2 초**). 그런데도 완주한 이유는 **F-10 의
결승선 포착**(`8bbcb94`)이 0.2 초여도 안 놓치고, HOLD 가 오버슈트를 `hold_timeout=30 s`
안에 되돌리기 때문이다(최악 17.6 s). **F-11 은 유효한 결함이지만 우선순위는 낮다.**

### 9-2. 🔴 깨지는 것은 공간이다 — 역천이 오버슈트가 **~70 m 에서 포화**

**편도를 150 → 25 m 로 6배 줄여도 총 수평 이탈은 188 → 102 m, 절반밖에 안 준다.**
⇒ **비행 방향 120 m × 폭 40 m** 가 없으면 편도 축소로는 해결되지 않는다.
줄일 것은 경로가 아니라 **"FW 로 날지 여부"** 다. 횡방향도 짧을수록 벌어진다(40 m 런 −17.2 m).

### 9-3. `transition_alt=25` 가 F2 를 눈에 띄게 쉽게 만든다

네 런 **모두 `VISION_SEARCH` 진입 0.2~0.6 초 만에 래치**(24.8~25.0 m, 상한 33.6 m 아래).
**정렬·나선·재탐색이 한 번도 필요 없었다.** ⇒ **F2 가 실패하면 원인은 상태기계가 아니라 검출이다.**

### 9-4. 결함 실증 (`node.log` 원문 확인, 세션 자기보고 아님)

- **F2-f** `탐색고도 정렬 타임아웃 30s 초과 (AGL 41.8m/25m, WP1 거리 30.4m) → 나선 강제 진행`
- **F2-c** `vision 생산자 사망(link ERROR) — AGL 23.6m → GPS 착륙 폴백` 이 **13 s** 에 발화(시한 60 s)
- **F2-g** `인계고도 도달 (3.5m AGL, 목표 3.0m±0.5) → LANDING` — V2 의 22 초 정지가 사라졌다

### 9-5. 미실시

- **편도 20 m 이하** — `d_end_thresh=10 m` 라 FOLLOWING 0 틱 가능성. **가지 말 것.**
- 편도 80/100 m(60 m 완주로부터 단조 추정)
- `vision_landing=false` 전용 종점 완주 런

### 9-6. 실기체 사전 검증 (오케스트레이터 직접 확인)

`fc_bridge` PYTHONPATH 사각지대 통과 — **`handoff_due(3.1, 3.0, False) = True`** 를
md5 가 아니라 **함수 호출로** 확인했다(F2-g 가 실기체에 실제로 들어가 있다).
`fc_ros` `install/` ↔ `src` md5 일치. shim 컨테이너 기동·1 Hz 재접속·SIGTERM 정상 종료.
**`picam-venv` 는 `~/picam-venv` 가 아니라 `/home/suri/local-libcamera-src/picam-venv` 다.**

---

## 10. 정리 대기

WSL `Ubuntu-22.04` 에 폐기 클론이 쌓여 있다(약 2 GB, 디스크 여유 928 G 라 급하지 않음):
`/root/ws_r5`(340 M) · `/root/ws_r5b`(727 M) · `/root/ws_f2`(503 M) · `/root/ws_f2b` ·
`/root/ws_c1b` · `/root/clone_presync_backup_20260729`(167 M).
`/root/drone_ws` 와 `/root/PX4-vehicle` 은 **무접촉**이므로 건드리지 말 것.
