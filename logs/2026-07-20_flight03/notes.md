# 2026-07-20_flight03

- **비행 조건:** (기체/모드/launch 인자: vehicle_type:=mc transition_alt:=5.0 waypoints:=[0.0,0.0,5.0, -4.24,-4.24,5.0, 0.0,0.0,5.0])
- **관찰:** throthle loss, cant incline
- **결론 (2026-07-20, pyulog 분석, `log_24_...ulg` 기준, 20.5초) — 사용자 보고 ①②(추력부족+우측 접지 후 전복)가 이 로그에서 그대로, 훨씬 심하게 재현됨:**
  - **이륙 자체가 실패.** `vehicle_local_position` 기준 AGL이 전 비행 -0.01~-1.35m로 **한 번도 순양(+)으로 못 올라감** — 목표 5.0m는 물론 지면조차 제대로 못 떠남.
  - **roll 붕괴 타임라인(정밀 추적):** t=0~2.3s 정지 상태(roll≈-5.5°, `ground_contact`=1). t=2.3s `ground_contact`→0(이륙 시도 시작, AUTO_TAKEOFF). **t=3.7s→6.1s(약 2.4초) 사이 roll이 0°→84°로 폭주**(우측이 바닥 쪽으로) — PX4는 이 와중 t=4.4s에 스스로 AUTO_LOITER로 이탈(자체 이상감지, 폭주 저지는 못함). 84° 부근에서 잠시 정체(우측을 대고 누운 자세로 추정)하다 **t≈12.0s에 roll이 180°를 넘어가며 사실상 완전 전복**(-179.8°→+178.8°로 랩어라운드), t=15.02s에는 pitch도 -79.9°까지 곤두박질 — "뒤집힘" 보고와 정확히 일치.
  - **핵심 발견 — 이건 소프트웨어 세트포인트 버그가 아니라 "쓰고도 못 이긴" 물리적 열세:** `vehicle_attitude_setpoint`(`q_d`)를 디코드하면 이 구간 내내 목표 roll은 계속 **0° 부근(±7° 이내)**으로 정상적으로 수평 유지를 명령하고 있었음(flight01 사고 때의 절대좌표 순간점프 같은 세트포인트 결함 없음). 그런데도 실제 roll은 폭주 — 대응해 컨트롤 얼로케이터가 **우측 모터(FR=motor0, RR=motor3, `CA_ROTORx_PY`=+1)를 갈수록 증대해 최대(RR 1.00 고정, FR 0.85)로, 좌측 모터(RL=motor1, FL=motor2, PY=-1)를 갈수록 삭감해 0.00까지 완전히 죽여** roll을 되돌리려 했으나 **롤레이트가 오히려 최대 178°/s까지 가속** — 즉 좌우 모터를 낼 수 있는 최대 차동(우측 풀, 좌측 제로)까지 다 썼는데도 못 이긴 것. 얼로케이터/컨트롤러 로직 자체는 정상 대응이었음.
  - **원인 후보(미확정, 다음 세션 물리점검 필요):** (a) 좌측 모터(RL/FL) 실추력이 지시치 대비 약함(프롭 손상·ESC 열화·모터 축 문제 등, flight01 사고 당시 조종사 RC 급회수 과정에서 손상됐을 가능성 포함) (b) 배터리 전압붕괴로 전 모터 최대추력 자체가 낮아져 정상 크기의 좌우 불균형도 못 버틴 것(아래 종합 참조) (c) 이륙 지점 지면이 고르지 않아 다리 한쪽이 먼저 걸리는 기계적 요인. **모터별 실측 편차(정적 스로틀 스윕)로 (a) 배제/확정 필요 — 다음 지상 점검 우선순위.**
  - **배터리:** 부하 중 최저 9.76V(3S, 셀당 3.25V — flight02보다는 양호), 전류 최대 26.3A, `remaining` 시작 0.64. flight02 종료(remaining 0.00) 직후인데 flight03이 0.64로 시작 — **팩 교체 가능성 있음(미확인, 다음 세션에서 사용자 확인 필요)**.
  - **정정 (2026-07-21, 사용자 재질의로 재분석) — t=4.41s LOITER 진입은 조종기 입력이 아니었음:** `vehicle_status.nav_state_user_intention`(사람이 마지막 선택한 모드) 대조 결과 t=4.41s는 `nav_state`만 4(LOITER)로 바뀌고 `user_intention`은 계속 17(TAKEOFF), `failsafe`=1 — **PX4 자체 페일세이프 강제전환 확정, 조종기 아님.** 실제 조종기로 제어권을 가져온 시점은 **t=9.35s**(`nav_state`·`user_intention`이 동시에 2=POSCTL로 전환, `failsafe`=0) — 이 시점엔 이미 roll≈81°(옆으로 누운 상태)였음. 즉 **t=3.7~6.1s의 최초 롤 폭주(0°→84°)는 100% 소프트웨어 제어 구간(AUTO_TAKEOFF+페일세이프-LOITER)에서 일어났고, 조종기 개입은 그 이후의 반응**이었음(폭주를 유발한 게 아님). t=12.68s에 다시 `failsafe`=1(LOITER 강제, 이때 `user_intention`은 POSCTL 유지)로 페일세이프가 재발동 — 완전전복(t≈12s)과 겹치는 시점.
  - **얼로케이터 포화 재해석(2026-07-21) — "카운터-보정 자체가 낮은 achieved 값을 만든다"는 사용자 의혹 확인됨:** `control_allocator_status.torque_setpoint_achieved`는 roll/pitch/yaw/thrust 통합 플래그라 roll 전용 판단엔 부적합 — **`unallocated_torque[0]`(roll 축 전용 미달성 토크)로 재검증.** 결과: **t=0.5~5.16s(roll -5.6°→+28.7°로 서서히 증가하는 구간) 내내 `unallocated_torque[0]`≈0 — 요구 토크를 100% 달성, 포화 전혀 없었음.** 처음 유의미하게 벌어지는 시점은 **t=5.37s(roll 33° 도달 시)**, 이후 t=5.9~6.1s에 롤레이트 178°/s로 폭발. **즉 "얼로케이터가 최대치를 다 썼는데도 못 이김"은 롤 오차가 이미 30°를 넘은 뒤부터의 현상이지, 폭주를 일으킨 독립적 원인이 아님** — 지난 결론의 "얼로케이터/컨트롤러가 최대 차동까지 다 쓰고도 못 이김 = 하드웨어 이상의 증거"라는 프레이밍은 인과관계 과장이었음, 정정.
  - **진짜 트리거는 더 이른 시점으로 이동:** `vehicle_angular_velocity`(roll rate) 대조 결과 **t≈3.63s**에 롤레이트가 잡음 수준(±2°/s)에서 갑자기 16~22°/s로 튐 — 이 순간 컨트롤러의 요구 토크(`vehicle_torque_setpoint` x축)는 여전히 ±0.1 수준(정상 트림)이고 포화도 없었음(요구한 만큼 다 만들어짐). `ground_contact`가 0으로 바뀐 시점(t=2.3s, 이함 시작)으로부터 약 1.3초 뒤. **정적인 좌우 추력편차라면 이함 즉시(t=2.3~2.4s)부터 트림 이상이 드러났어야 하는데 그러지 않고 1.3초 뒤 갑자기 발생** — "상시 존재하는 고정 편차"보다 "이함 트랜지언트(다리가 비대칭적으로 지면에서 떨어짐, 또는 RPM 상승에 따른 프롭 진동이 그 순간 임계치를 넘음)"에 조금 더 무게가 실림(미확정, 물리 재현 필요).
  - **PX4 내장 진동지표 참고:** `failure_detector_status.imbalanced_prop_metric`(프로펠러 불균형 진동 지표)이 비행 내내 단조 증가(최대 절대값 10.4) — `fd_imbalanced_prop` 하드 트리거(`FD_IMB_PROP_THR`)엔 못 미쳤으나 추세 자체는 실측됨.
  - **다음 세션 우선순위 갱신:** 정적 모터 벤치 테스트(좌우 추력편차 정량화)는 여전히 유효하나, 그 못지않게 **이함 트랜지언트 자체를 저속촬영/테더로 재현·관찰**(다리 이탈 비대칭·RPM별 진동 급증 여부)하는 항목을 추가 — 트리거가 "정적 편차"가 아니라 "이함 순간의 동적 사건"일 가능성이 이번 재분석으로 커짐.
