---
doc_type: hypothesis_tracker
project: suridoksuri-1
scope: "🚁 mc-실기체 트랙 — 반복되는 실비행 이상현상(추력부족·이함직후 롤폭주)의 열린 가설 목록"
last_updated: 2026-07-30
---

# mc-실기체 이상현상 — 열린 가설 목록

> **이 문서의 목적:** "비행 → 로그수집 → 분석 → 평가 → 비행" 사이클을 반복할 때마다
> 가설공간을 처음부터 다시 만들지 않기 위한 것. 새 비행을 `analyze_flight.py`로
> 분석한 뒤, 그 결과를 아래 가설들과 대조해서 **상태(status)만 갱신**한다.
> 상세 수치·타임라인은 여기 옮겨적지 않고 해당 flight의 `notes.md`/`analysis_auto.md`를
> 인용한다 — 이 문서는 "지금 뭘 믿고 뭘 안 믿는지"의 요약만 유지한다.
>
> **갱신 절차:** ① 새 분석 결과가 어떤 가설을 지지/반증하면 그 행의 상태·근거를
> 한 줄로 갱신 ② 완전히 새로운 원인후보가 떠오르면 새 행 추가(카테고리 A~D 중
> 적절한 곳에) ③ 상태가 바뀐 것만 아래 "이력"에 한 줄 추가.

---

## 현상 요약 (반복 관찰된, 변하지 않는 사실 — 상세는 각 flight notes.md)

- 이함(liftoff) 직후 우측으로 roll하는 경향이 여러 비행에서 반복 관찰됨
  (flight02/flight03/수동 log_28/log_29 — 표본 작음, "항상"까지는 확정 못 함)
- flight03에서: 이함(`ground_contact`→0) 약 1.3초 뒤(t≈3.6s) 롤레이트가 잡음수준
  (±5~8deg/s)에서 갑자기 튐 — 이 순간 요구토크는 작고 얼로케이터 포화도 없었음
- 얼로케이터(`unallocated_torque[0]`)는 roll 오차가 27~33도를 넘기 전까진 두 비행
  모두 전혀 포화되지 않음 → **포화 자체는 원인이 아니라 오차가 커진 뒤의 결과**
  (이전에 "포화=하드웨어 이상 증거"로 봤던 것은 정정됨, `docs/session_log.md`
  2026-07-21 항목 참조)
- 같은 세션 배터리는 매 비행 부하중 8.5~9.8V까지 붕괴(3S 휴지전압 11.1~11.9V)
- 목표고도 미달(추력부족)도 같은 세션 내 반복 관찰

---

## 가설 테이블

| ID | 분류 | 가설 | 상태 | 근거(핵심만) | 다음 검증 |
|----|------|------|------|------|-----------|
| H1 | 전기 | 배터리 노후화/내부저항 증가 | 미검증(정황지지) — **2026-07-21 flight09 재현은 반증력 없음(정정)** | 매 비행 부하중 8.5~9.8V까지 붕괴 실측. flight09에서 두 번째 팩으로 교체 직후에도 부하중 8.61V/39.3A 붕괴 재현됐으나, **사용자 확인 결과 이 "교체 배터리"도 신품이 아니라 기존에 같이 쓰던 두 팩 중 나머지 하나**(둘 다 이미 사용 이력 있는 팩) — "신품 대조군" 실험이 아니었으므로 H1을 반증하는 증거가 못 됨(오히려 두 팩이 함께 노후됐을 가능성과도 정합). **일시 하향했던 "약한 반증" 판정은 이 근거오류로 철회.** | 진짜 신품(미사용) 팩 또는 배터리 IR/용량 실측기로 직접 측정해야 결론 가능 |
| H2 | 전기 | 배터리 용량이 기체 대비 원래 부족 | 미검증 — **2026-07-21 flight09 재현은 반증력 없음(정정)** | H1과 데이터로 구분 안 됨. flight09 근거가 H1과 동일 사유로 무효화됨(위 H1 정정 참조) | 진짜 신품/타사양 팩 교체 후 동일증상 재현 여부(아직 미수행) |
| H3 | 전기 | 배선/커넥터 저항 증가 | 미검증 | 근거 없음(추정만) — **2026-07-21에 "신품 배터리도 동일 재현"을 근거로 우선순위를 올렸었으나, 그 전제(신품 여부)가 틀렸음이 확인돼 이 근거도 철회.** 원래 상태(근거 없음)로 복귀 | 커넥터 육안점검 + 저항 실측 |
| H4 | 전기 | ESC별 저전압컷오프 임계값 편차 | 미검증 | 근거 없음 | 개별 ESC 스펙/펌웨어 확인 |
| H5 | 기계 | 좌측(RL/FL) 모터·프롭 실추력 저하 | **재정의됨 → H13으로 흡수(2026-07-21)** | `imbalanced_prop_metric` 비행 내내 단조증가(트리거 임계값 `FD_IMB_PROP_THR`=30 미도달) — 그러나 실제 모터커맨드 평균을 축별로 뜯어보니 "좌/우"가 아니라 "CW/CCW 대각선" 축으로 갈리는 게 훨씬 뚜렷함(H13 참조), 이 축의 "좌측" 프레이밍은 부정확했던 것으로 정정 | H13으로 이관 |
| H6 | 기계 | 모터 마운트/베어링 손상 | 미검증 | 근거 없음 | 육안+손 점검(축 유격) |
| H7 | 기계 | 프롭 회전방향 오조립 | 낮은 우선순위 | 근거 없음(실장착과 `CA_ROTORx_KM` 대조 안 해봄) | 실장착 회전방향 vs 파라미터 대조 |
| H8 | 기계 | **무게중심(CG)이 전방·우측으로 편향 — 임계 모터가 먼저 포화해 나머지 모터의 상승여력이 통째로 사장된다** | **🔴 확정·정량화됨(2026-07-30). H13을 대체하는 정답. 지상 조치 우선순위 2번** | **2026-07-30 f02 안정호버 61초**(자세가 거의 수평 = 자세지령이 아닌 **상시 트림**): 앞(m0 전우+m2 전좌)/2 = **0.795** vs 뒤(m1 후좌+m3 후우)/2 = **0.639** → **추력비 1.55배**, 우 0.765 vs 좌 0.669 → 1.31배. **CW/CCW 대각선 차이는 −0.014로 0**(H13 기각). 모멘트 평형 역산 시 기하중심 대비 **앞으로 세로 축간거리의 약 11%, 우로 가로 축간거리의 약 7%**. **대가:** m0 전우가 0.836에서 천장에 먼저 닿을 때 m1 후좌는 0.584로 **0.42의 여유를 사장**한다 — 기체 전체의 상승능력이 가장 혹사당하는 모터 하나에 묶여 있다. f04가 가장 선명(m0만 1.00 고정, m1은 0.61). 넷을 0.72 근방으로 균등화하면 임계 모터 여유가 **+0.16** 늘어난다. 상세: `logs/2026-07-30_review.md` §6 | ①**저울/수평계로 CG 실측 후 배터리·페이로드를 뒤로·왼쪽으로 이동**(비용 거의 0, T/W를 안 건드리고 얻는 최대 이득) ②이동 후 재비행해 네 모터 평균이 0.72 근방으로 모이는지 확인 ③그래도 앞/뒤 차가 남으면 그때 모터·프롭 개체차(정적 추력벤치)로 넘어갈 것 |
| H9 | 기계 | 이륙면 불균일(다리 한쪽 늦게 이탈) | 약한 반증 | flight03 정지시 roll -5.5°(좌측 기움)였다가 우측으로 폭주 — 기움 방향과 폭주 방향 불일치 | 평탄면에서 재현 시도 |
| H10 | 제어 | 극단자세에서 EKF/오일러각 왜곡이 확산을 증폭 | 약한 지지(2차 기여로만) | 최초 트리거(t≈3.6s, roll<10°)는 왜곡 영역(±60°+) 밖에서 시작 — 원인이 아니라 후반 증폭요인 후보 | 낮은 우선순위 |
| H11 | 제어 | 얼로케이터 기하 파라미터(`CA_ROTOR*`)가 실제 프레임과 불일치 | 거의 기각 | 파라미터가 표준 X쿼드 배치와 일치, 모드전환·회복거동도 정상적(비정상 매핑이면 안 나올 패턴) | 낮은 우선순위 |
| H12 | 결합 | **이함 트랜지언트(다리 비대칭 이탈 또는 RPM 상승에 따른 프롭진동 급증)가 진짜 트리거, 배터리 약화가 회복여유(포화 마진)를 깎아 못 잡음** | 최유력(H13 발견 전) | ①t≈3.6s 트리거가 이함 후 1.3초 지연 ②오차 작을 때 포화 없음(H5·H1 단독으론 이함 즉시부터 나타났어야) ③전류 높을수록(전압 붕괴 심할수록) roll 불안정도 심함(수동 5건 상관관계) | 이함 순간 저속촬영/테더 재현 관찰 |
| H13 | 기계 | **CW 대각선(motor0 전우+motor1 후좌, `CA_ROTOR_KM=+0.05`) vs CCW 대각선(motor2 전좌+motor3 후우, `KM=-0.05`) 간 추력 비대칭 — CCW 쌍이 구조적으로 더 많이 일함** | **🔴 기각 확정(2026-07-30)** — 대각선 축의 차이는 0이다. H8로 대체 | **2026-07-30 f02 안정호버 61초(t=44~105s, OFFBOARD 제자리, 자세 roll+0.66°/pitch−2.02°)로 종결:** CW대각(m0,m1)=**0.710** vs CCW대각(m2,m3)=**0.724** → 차 **−0.014 (추력비 1.00배 = 차이 없음)**. 같은 데이터에서 **앞(0.795) vs 뒤(0.639) = 추력비 1.55배**, 우(0.765) vs 좌(0.669) = 1.31배로 **비대칭의 진짜 축은 전/후(주)와 좌/우(부)** 임이 드러났다. 07-21에 "CCW쌍이 더 일한다", 07-25에 "CW쌍이 더 일한다"로 두 번 뒤집혔던 것은 **대각선 축이 애초에 실재하지 않아 표본마다 부호가 흔들린 것**이다. 개별 모터 순서(m0 전우 0.836 > m2 전좌 0.754 > m3 후우 0.694 > m1 후좌 0.584)는 전방+우측 CG 편향의 교과서적 패턴이다. → **H8로 이관.** 상세: `logs/2026-07-30_review.md` §6 **2026-07-25 3비행(수동#1·수동#2·flight04)에서 비대칭 방향이 반대로 나왔다:** CW쌍(m0,m1) 0.718~0.803 vs CCW쌍(m2,m3) 0.627~0.706 (CCW/CW = 0.80~0.89배). 게다가 대각선으로 깔끔히 갈리지 않고 **motor0 단독이 튄다**(0.857/0.692/0.905, 나머지 ~0.70). `actuator_saturation`은 m0=+2(상한 포화), m1·m3=−2(하한 포화) — 얼로케이터가 m0을 계속 밀어올리며 나머지로 상쇄하는 상시 편향. 이 사이 배터리를 3S→4S로 바꿨으므로 무게·CG가 달라졌을 수 있다. → "CW/CCW 대각선 축" 프레이밍 자체가 안정적이지 않다. 상세: `logs/2026-07-25_manual/notes.md`. (이하 2026-07-21 근거) 같은날 서로 다른 배터리 팩을 쓴 flight04·flight08·flight09 **셋 다** motor0+1 평균 커맨드 0.45~0.51 vs motor2+3 평균 0.67~0.76(약 1.4~1.5배)로 **동일 방향·유사 배수 재현**, `imbalanced_prop_metric_max_abs`도 세 비행 모두 동일부호(-16~-34). 배터리 종류와 무관하게 재현되므로 배터리 원인 배제, CCW쌍(전좌·후우)이 이미 포화 근접(mean 0.7~0.81)이라 상승여력 병목·부하중 고전류의 직접 원인일 가능성. flight09의 "지상에서 무입력 roll 요동"도 이 상시존재 비대칭이 이함 시도 중 미세변동에 취약하게 만든 것으로 설명 가능(추정). H5(좌/우 프레이밍)를 대체·구체화, H8(CG 편향)과는 로그만으로 구분 불가(같은 패턴 발생) | ①모터별 정적 추력벤치(CW쌍 vs CCW쌍 분리 측정, 최우선) ②CW쌍 내부·CCW쌍 내부 프롭 상호교체로 개별 프롭 결함 여부 확인 ③CG 저울 실측으로 H8과 분리 |
| H14 | 제어 | **PX4 자세/각속도 PID 게인(`MC_ROLLRATE_P/I/D`, `MC_PITCHRATE_P/I/D`, `MC_ROLL_P`/`MC_PITCH_P` 등)이 이 커스텀 기체에 맞게 한 번도 튜닝된 적 없음(기본값 그대로일 가능성) — 게인 부족으로 초기 각속도 오차를 못 잡아 오차가 커진 뒤에야 얼로케이터가 반응, 그땐 이미 발산 국면** | **부분 검증됨(2026-07-25) — 레이트 게인은 기본값 확정** | **2026-07-25 log_152 파라미터 실측:** `MC_ROLLRATE_P=0.15 / I=0.20 / D=0.003`, `MC_PITCHRATE_P=0.15 / I=0.20 / D=0.003` = **PX4 기본값 그대로**(우선순위 0번 확인 완료). 다만 `MC_ROLL_P=5.0`, `MC_PITCH_P=4.0`은 기본값(6.5)에서 **낮춰져 있다** — 누군가 자세 P만 건드린 이력이 있다는 뜻이므로 "한 번도 안 건드림"은 정확하지 않다. 레이트 루프는 미튜닝 확정. ①H12/H13 재분석에서 공통으로 나온 패턴: t≈3.6s 트리거 이후 roll 오차가 27~33°로 커질 때까지 `unallocated_torque[0]`≈0 유지 — 게인이 이 기체 관성/추력특성에 맞게 충분했다면 오차가 작을 때부터 비례+미분항이 크게 반응해 초기에 잡혔어야 함, 느린 초기반응 자체가 게인 부족의 전형적 시그니처 ②이 기체는 비표준 모터/프롭 조합(H13에서 CW/CCW 비대칭 이미 확인 — PX4 기본 게인이 가정하는 표준 프레임과 물리특성이 다름을 시사)인데 세션로그·트랙보드 어디에도 PID 튜닝을 수행했다는 기록이 없음(기본값 그대로 비행해왔다는 정황) ③H10(EKF 왜곡)·H11(얼로케이터 기하)은 이미 조사·대체로 기각되어 "제어" 분류에서 남은 미검증 후보가 사실상 이것뿐이었음 H12·H13과 배타적이지 않음(공존 가능) — H13(상시 비대칭 교란)이 있어도 게인이 충분히 세면 더 오래 버텼을 것이므로, 게인 부족은 "왜 못 버텼는가"를, H13/H12는 "무엇이 교란했는가"를 각각 설명하는 보완 관계로 봄 | ①**(비용 0, 최우선)** 현재 `MC_ROLLRATE_P/I/D`, `MC_PITCHRATE_P/I/D`, `MC_ROLL_P`, `MC_PITCH_P` 값을 FC에서 읽어 PX4 v1.14 기본값과 대조 — 동일하면 "한 번도 안 건드림" 사실상 확정 ②테더(안전줄) 상태에서 PX4 내장 온보드 오토튠(`mc_autotune_attitude_control`, roll 축부터 단독) 실행 — 상세는 `docs/session_log.md` 2026-07-22 항목 |
| H15 | 제어 | **`MPC_THR_HOVER`(0.5)가 실제 호버 스로틀(0.68~0.84)과 크게 어긋남 — 수직 제어의 추력 피드포워드가 실제보다 낮게 잡혀 과도구간 고도유지가 불리** | **정정됨 — HTE 동작 확인, "수렴 전 기동"으로 재정의(2026-07-25). 2026-07-30 실측 호버값 확정** | **2026-07-30 f02: `hover_thrust_estimate = 0.693`(전체의 83.3% 구간에서 valid) → T/W = 1.44 확정.** 그리고 이날 7비행 중 HTE가 valid가 된 것은 **f02 하나뿐** — 나머지 6건은 비행이 짧아 전 구간을 잘못된 `MPC_THR_HOVER=0.5` 피드포워드로 날았다("수렴 전 기동"이 상시 조건임이 재확인). `MPC_THR_HOVER`를 0.69로 직접 써 두면 HTE 수렴 전 구간의 오차가 사라진다. 2026-07-25 3비행 실측 비행중 평균 모터출력 median 0.675~0.789인데 `MPC_THR_HOVER=0.5`. flight01에서 10.85 m 상승 지령 중 오히려 −2.1 m/s로 하강했고, tilt는 최대 18.8°(수직추력 손실 5.3%)라 기울기로는 설명이 안 된다. 개별 모터가 비행 샘플의 23~37%에서 1.000 포화. `MPC_THR_MAX=1.0`. → 추력 절대부족(H1/H2/H13)과 피드포워드 오설정이 겹쳐 상승여력이 없는 상태. 상세: `logs/2026-07-25_flight01/notes.md` §4 | ①**(2026-07-25 확인 완료)** 온라인 추정기(HTE)는 **동작 중**이며 log_141에서 0.70으로 수렴 — 영구 오프셋은 아니다. 다만 `hover_thrust_estimate.valid`가 1이 되는 시점이 **arm 후 약 28초**였고, flight01은 arm 후 12초 만에 OFFBOARD에 진입했다 → **추정 수렴 전에 대기동을 시킨 것**이 문제. 가설을 "영구 오설정"에서 "수렴 전 기동"으로 재정의할 것. ②`SDLOG_PROFILE`에 `hover_thrust_estimate`를 항상 포함(flight04 로그엔 아예 없었다)하고, OFFBOARD 진입 조건에 `hover_thrust_estimate.valid==1` 대기를 넣을지 검토 ③`BAT1_CAPACITY=-1`·`BAT1_V_EMPTY=3.6V`로 부하중 sag이 곧바로 "Emergency battery"로 읽히는 것도 함께 정정 |
| H16 | 소프트웨어 | **(가설 아님 — 2026-07-25 확정된 결함)** `offboard_node`의 MC 경로추종이 웨이포인트를 **EKF 로컬원점 기준 절대좌표**로 발행한다(이륙지점 보정 없음). EKF 원점은 이륙지점과 무관하고 계속 흐른다 | **확정 → 수정 완료(2026-07-25, 실기체 미배포)** | flight01: 원점이 이륙지점에서 수평 10.94 m·수직 10.55 m 어긋나 있어 WP `[0,0,3]`이 실제로는 "10.9 m 옆 + 13.55 m AGL"을 지령 → 조종사 개입 → KILL. flight04: 같은 구조로 3.37 m 옆 + 5.11 m AGL(의도 3 m). 지상 정지 상태에서 로컬 z의 지면값이 22분 만에 4.5 m 이동, xy 원점 리셋 8→23회. 상세: `logs/2026-07-25_flight01/notes.md`. **배포 문제 아님이 확인됨** — 저장소/RPi소스/colcon build/install 네 곳의 `offboard_node.py` md5가 전부 동일(`a200d0d…`)하고, 지오이드 수정(`568fbe5`)이 도입한 런타임 문구가 오늘 로그에 3회 찍혔으며, flight04 이륙량도 3.07 m(수정 전이었다면 27.8 m)로 정상. **지오이드 수정은 ①CommandTOL 목표고도 축을 고친 것이고 ②cruise_alt ③수평 waypoints는 그 수정이 손댄 적 없는 별개 경로다.** `_takeoff_ground_h`는 이미 캡처되고 있으나 `climbing_reached()`에만 전달되고 ②③엔 미적용 — "로컬 원점≠지면"은 2026-07-07 `climbing_reached()` docstring에 이미 기록돼 있었다(당시 실측 2.11 m, 오늘 flight04도 2.11 m) | 코드 수정 — ①OFFBOARD 진입 시점 `state.pos_ned`를 경로 원점으로 삼아 waypoints를 상대해석 ②`cruise_alt`를 `_takeoff_ground_h + wp_z`로 ③로컬 z 대신 AMSL 기준 유지 ④`_step_following()`이 수동모드(POSCTL/MANUAL/ALTCTL)에서 OFFBOARD를 재요청하지 않도록. **→ ①②④ 구현 완료:** `waypoint_frame` 파라미터 신설(기본 `"takeoff"`, `"local"`로 종전 동작 복귀 가능), 이륙 순간 `_apply_path_origin()`이 경로 전체를 이륙지점 기준으로 평행이동, `_State.PILOT_TAKEOVER` 신설로 조종사 인계 시 세트포인트 발행 자체를 중단. pytest 286 통과, flight01 실측값 재현 검증(WP0 오차 10.93 m→0.00 m, 순항고도 13.55→3.00 m AGL). **남은 것: 실기체 배포(git pull + colcon build) 및 SITL 회귀검증 — 아직 안 함.** ③(로컬 z 대신 AMSL)은 미구현 → **2026-07-25 09:58 이전에 배포 완료됨이 로그로 확인(flight05부터 `경로 원점 = 이륙지점 … (frame=takeoff)` 출력). flight14·16이 전 구간 완주(경로이탈 최대 0.94/2.36 m)로 실기체 유효성까지 입증 — H16 종결.** ③만 잔여 |
| H17 | 센서 | **자기계(나침반)가 EKF에서 기각돼 절대 헤딩 기준이 없다 — yaw 추정이 부팅마다 수십 도씩 틀린다** | **확정 + 원인 규명 완료(2026-07-27). 🔴 2026-07-30 현재 미해결이며 `PRIO=0`(비활성)으로 악화** | **2026-07-30:** `CAL_MAG0_PRIO`가 07-25의 **75 → 0**(비활성)으로 바뀌었고 offset/off-diag도 재캘된 흔적이 있으나(YODIAG −0.182 → −0.105) 여전히 EKF `cs_mag_hdg = 0%`(수동비행만 34%). manual 로그에 `Compass needs calibration - Land now!` + `Preflight Fail: Compass 0 fault`. **조치 ④(해결 전까지 VTOL 천이 금지)를 어기고 f02에서 천이를 시도했다** — 다만 이번엔 GPS가 RTK급(fix=4, 위성 20~29, eph 0.16~0.74m)이라 헤딩오차 −10.4°에 그쳐 무해했고, 07-25 flight17의 65° 폭주는 재현되지 않았다. **정지 호버 구간의 yaw 드리프트 위험은 그대로.** flight16·17·19에서 `estimator_aid_src_mag` 기각률 **100%**, `cs_mag_hdg=0%` → flight17 MC→FW 천이에서 지령 방위 216.9°인데 실제 가속 150~160°(**60~67° 오차**), 조종사 긴급 인계. **원인 규명:** `sensor_mag`(캘리브레이션 적용 **전** 원시값) = **0.441 G**(광주 기준 0.503의 88%, 정상)인데 `vehicle_magnetometer`(적용 **후**) = 0.344 G(68%)로 떨어져 `EKF2_MAG_CHK_STR=0.2` 탈락. 저장값 `CAL_MAG0 offset=[+0.056,−0.143,+0.124]`(**\|offset\|=0.197 G = 지자기의 39%**), `off-diag=[−0.005,−0.182,−0.048]`(YODIAG −0.182는 정상 ±0.05 대비 비정상). **결정적: 보정이 산포를 줄이는 게 아니라 키운다** — 원시 ±10%(0.407~0.495) → 보정 후 ±18%(0.304~0.440). EKF 비행중 바이어스 학습이 그 위에서 계속 밀려남(`CAL_MAG0_YOFF` 7/23 −0.001 → 7/25 −0.143). **→ 하드아이언·푸셔모터 자석·강철부품·전류간섭은 근거 없음**(전류 상관 −0.07, 모터정지 시에도 동일, 원시 세기 정상). **"GPS선 알루미늄 호일 차폐(7/23·7/25, 무접지)가 원인"도 기각** — 원시 감쇠 자체가 없고, 알루미늄은 μr≈1이라 정자기장 차폐 불가(1 Hz 표피깊이 8.4 cm ≫ 호일 20 µm), 무접지 와전류가 만드는 자기장은 교류라 샘플 평균 0이며 5 cm에서 0.1 G를 내려면 2.5 A 필요(유도 와전류보다 3~6자릿수 큼). 상세: `logs/2026-07-25_review.md` §5·§5-1 | ①**나침반 6면 재캘리브레이션**(최종형상·차폐 유지·개활지, 6면을 실제로 다 채울 것 — YODIAG 이상은 회전 커버리지 부족의 전형) ②검증: 지상 360° 회전 중 `vehicle_magnetometer` 0.45~0.55 G·변동 ±10% 이내(원시값과 근접) + `test_ratio<1` ③2번 자기계(396321, 원시 1.4~2.0 G로 엉터리·`PRIO=0`) 배선·모델 점검해 복구 → 교차검증 확보 ④**해결 전까지 VTOL 천이 금지** ⑤`EKF2_MAG_CHK_STR`를 키워 검사를 통과시키는 우회는 금지(틀린 헤딩을 EKF에 먹이는 것) |
| H18 | 센서 | ~~**피토 차압 영점이 온도에 따라 1.91 Pa/°C 이동한다**~~ → **온도는 원인이 아니다. 영점 15.7 Pa 이동의 원인 미상** | **🔴 2026-08-01 지상 벤치 실험으로 온도 원인 기각 (`logs/2026-08-01_pitot_thermal/notes.md`).** 사전등록 절차서(`docs/fc_pitot_thermal_experiment.md` §3) 기준 정식 충족 — 9시간 54분·352,163 표본·청정 온도폭 **13.58 °C**(요건 ≥12)에서 `dp ~ 1 + T + t` → **`b_T = +0.027 Pa/°C [95% CI +0.024, +0.029]`**, `b_t = +0.05 Pa/h`, `corr(T,t) = −0.483`(밤샘 강온이 승온 구간의 공선성 0.95를 깨줬다). **CI 가 −1.3 을 400 표준오차 밖으로 배제**하고, 계수를 액면대로 받아도 12 °C 에서 0.32 Pa = 15.7 Pa 의 **2 %** 다. 분할 추정 전부 −0.5~+0.12 로 −1.387 근처가 없다(가장 음수인 −0.341 조차 4.1 Pa 만 설명). 독립 확인 3건: 냉방 강온 −2.74 °C→예측 +3.80/실측 **−0.49**, 오븐 승온 +2.13 °C→예측 −2.95/실측 −0.49, 아침 개방 +5.31 °C→예측 −7.37/실측 **+1.51**. 정착 앵커 3개(22.418/−13.376, 22.439/−13.597, 26.148/−10.925, 각 se≈0.04)가 **온도 순서로 정렬조차 안 되고 산포 2.7 Pa**(−1.387 이면 15 Pa 여야). ⚠️ 중간분석에서 `b_T=−1.602` 가 나와 "실비행 계수 재현"으로 보고할 뻔했으나 **분할검정에서 전반 −2.003/후반 +2.603 으로 부호가 뒤집혀 기각**했다 — 공선성 구간의 다중회귀를 그대로 믿으면 안 된다. **🔴 이로써 아래 조치 ①("현장에서 열평형 후 캘")은 무효다** — 온도가 원인이 아니면 온도를 맞춰도 낫지 않는다. **남은 가설: W(수분) 는 미검정**(조건 미성립 — 실내 이슬점 통제 실패·습도계 부재·기울기 시험을 노출 +21분에 수행해 과도현상 창을 놓침. 단 **건조 상태 음성 대조군은 확보** — 기울기 10° 무반응, 검출한계 1.0 Pa, 물 있으면 수십 Pa 예상), **M(배관 물리변화) 도 미검정**. **🔴 구조 결함 확정: 피토관에 배수구가 없고 배관 두 줄 모두 U자 트랩이라 응축수가 고이면 나갈 길이 없다.** 물이 고인 U자관은 마노미터라 양쪽 수위차가 그대로 차압이 된다 — **1.6 mm = 15.7 Pa**(내경 3 mm 에서 **0.011 mL**). 원인 규명과 무관하게 배수구 작업은 해야 한다. (이하 07-31 이전 서술은 이력 보존) | 지상 정지 raw = **−42.6 Pa**(07-30 전 비행)인데 저장 영점 `SENS_DPRES_OFF = −21.78` → 정지 상태 CAS **−5.9 m/s**. **로그 10개 회귀 `raw_zero = +25.65 − 1.906·T`(r²=0.956)를 근거로 온도 탓이라 했으나, 재검토 결과 이 회귀는 신뢰할 수 없다:** ①표본이 저온군(27~30 °C, 7점, 07-25~29)과 고온군(35.6~37.6 °C, 3점, **전부 07-30**) 두 덩어리이고 사이가 비어 있어 **사실상 두 점 추정**이며 r²는 군집 간격이 만든 값이다. **온도와 날짜가 완전 교락**돼 '온도가 높아서'와 '07-30이라서'를 구분할 수 없다. ②**군 내부에서는 온도효과가 안 보인다** — 저온군 −0.69(p=0.25), 고온군 +0.22(p=0.68), 07-30 하루 −0.60[−2.20,+1.00], **f02 정지호버(기류 교락 없음) +2.40[−0.75,+5.55]**, f05 −4.88[−10.08,+0.32]. 모든 군내 T폭이 2~3 °C 뿐이라 기대신호 4~6 Pa가 잡음 ±9~18 Pa에 묻힌다(검정력 0). ③**데이터시트에 °C당 계수가 없다** — MS4525DO는 Total Error Band <1.0 %FSS 하나로 규정하고 그 안에 열오차·정확도·영점공차가 전부 포함된다. 1 psi 계열이면 1 %FSS≈69 Pa라 **15.7 Pa는 0.23 %FSS로 스펙 내**다(온도 인과를 지지도 금지도 안 함). ④**'캘 온도 25.4 °C가 차량 실내와 맞는다'는 순환이었다** — 기울기를 −0.6~−4.0 어디로 가정해도 캘 온도가 18~27 °C로 나와 전부 차량 실내로 설명된다. **확실한 것:** 원시 영점이 4개 날짜(07-25/26/28/29) −29.0±1.2 Pa → 07-30 8개 로그 −44.7±0.5 Pa로 **15.7 Pa(날짜간 산포의 13배) 이동**했고 그 결과 정지 CAS −5.9 m/s가 천이 게이트를 굶겼다. **미입증:** 그 원인이 온도라는 것. 경쟁 가설 — **(a) 결로**(냉방 25 °C 차량 → 습한 옥외 33 °C, 피토 배관 수분은 영점 이동의 고전적 원인. 30 °C/70 %면 이슬점 23.9 °C, 80 %면 26.2 °C라 **기상청 과거자료 조회만으로 물리적 가능성 판별 가능 — 비용 0**) **(b) 이송 중 배관 꺾임·재체결**. 세 가설 모두 **조치는 동일**하므로, 실험의 가치는 조치를 바꾸는 데 있는 게 아니라 **이 피토를 천이 게이트로 계속 믿을지(조치 ④)를 결정**하는 데 있다. 사용자 제보로 확정 — 캘을 **에어컨 끈 차량 트렁크**에서 했다. 저장값 −22.79 는 회귀선상 **25.4 °C**, 직전값 −21.78 은 **24.9 °C** 에 정확히 대응하고 비행 시 센서온도는 **35~38 °C** 였다. **→ 캘 절차 자체는 옳았다(장비는 그 순간의 진짜 영점을 정확히 잡았다). "무풍"은 맞췄으나 "온도"를 못 맞춘 것이고, 같은 실수가 최소 2회 반복됐다.** 추가로 ①**비행 중 센서가 7 °C 식어**(37.8→30.8, 프롭 기류) 영점이 비행 중에만 +13 Pa 이동 — 지상 한 점 영점은 공중에서 무효. PX4 엔 차압센서용 `TC_*` 온도보상이 없다. ②신호 잡음이 ±12~18 Pa(겉보기 ±4~5.5 m/s)이고 로깅 1.0 Hz — **영점오차가 잡음의 1.7배뿐이라 이 피토로 12 m/s 임계 판정 자체가 무리다.** **정정:** 초판의 "영점 또는 VT_ARSP_TRANS 둘 중 하나만 고쳤어도 천이 성공"은 과장 — 온도보정 실제 대기속도는 **10~11 m/s**(대지속도 11.2 − 바람 1.1과 정합)라 영점을 고쳐도 12 엔 미달, 10 이어도 간신히 걸친다. **천이 실패는 H19(추력부족)의 또 다른 얼굴이다** — 푸셔 100 %를 10초 써도 10~11 m/s 에서 멈춘 것은 리프트 로터가 계속 0.4~0.6 으로 기체를 들고 있어야 해서다. 상세: `logs/2026-07-30_review.md` §7-4~7-8 | ⓪**🔴 최우선: 누락 로그 `log_270~273` 을 SD카드에서 회수** — 269(01:15 UTC)와 274(02:56 UTC) 사이가 비어 있고 **캘이 정확히 그 구간에서 일어났다.** FC가 켜져 있었다면 캘 당시의 `differential_pressure.temperature`·원시값이 그대로 들어 있어 순환논증을 깬다 ⓪-2**🔴 지상 열스윕 실험 — 절차서 `docs/fc_pitot_thermal_experiment.md` 에 사전등록 완료(판정기준 포함).** 에어컨 18 °C ↔ 외기 평형으로 센서 T를 12~15 °C 폭으로 **양방향 연속** 스윕하며 원시값 로깅(`SDLOG_MODE=2`, 캘 금지, 피토 커버로 기류 차단). 실측 잡음 기준 **ΔT 15 °C·30분이면 기울기 95%CI ±0.20 Pa/°C** — 지금 데이터(±3~5)의 30~50배라 0과 −1.9를 압도적으로 가른다. 비행 불필요·90분·소모품 0 ①**현장에서 열평형 후 캘**(FC 켠 채 그늘 5~10분, `differential_pressure.temperature` 안정 후). **냉방된 실내·차량 금지** ②**캘 직후 10초 검증: 정지 상태 `airspeed_validated.calibrated_airspeed_m_s` ≈ 0 인지 확인** — 이것만 했으면 07-25 이래 매번 걸렸다 ③`offboard_node` 에 천이 전 프리플라이트 게이트(지상 정지 \|CAS\| > 1.5 m/s 면 천이 거부) — 사람 기억에 의존하지 않는 유일한 방법 ④중기: 대지속도/개루프(`VT_F_TR_OL_TM`) 천이로 전환하거나 센서 교체(SDP3x 계열)·튜브 배치 재검토 ⑤⚠️ **중량 실측 전에는 `VT_ARSP_TRANS` 를 내리지 말 것** — `FW_AIRSPD_STALL=7` 은 설계중량 기준이고 실속속도는 중량의 제곱근에 비례한다 |
| H19 | 설계 | **추력대중량비 T/W ≈ 1.44 자체가 근본 제약 — 어떤 게인/파라미터로도 못 고친다** | **🔴 신설·확정(2026-07-30). 위 모든 가설의 상위 원인** | f02 `hover_thrust_estimate = 0.693` → **호버에 최대추력의 69%를 쓴다**(멀티콥터 관례 T/W≥2.0, 1.5 미만은 제어여유 없음). 실측 상승률이 지령(`MPC_TKO_SPEED=1.5 m/s`)의 절반 이하: f03 0.83 / f04 0.63 / f05 **0.44** / f07 0.86 m/s — 그때 네 모터는 전부 0.8~1.0. f05·f07은 **AUTO_TAKEOFF 상태에서 지령이 상승인데 기체가 내려온다**(무지령 강하). f07은 14.1 m에서 침하해 접지, 충격 피크 **8.2 G**. 14 m 자유낙하는 16 m/s인데 5.8 m/s에 그쳤으므로 **동력 상실이 아니라 추력 부족에 의한 침하**다. **만충 팩(4.22 V/셀)이던 f03조차 0.83 m/s** — 배터리를 고쳐도 T/W 1.44는 남는다. 상세: `logs/2026-07-30_review.md` §2 | ①기체 실중량 측정 + 모터/프롭 정적 추력벤치로 T/W 절대값 확정 ②감량(불필요 페이로드·배선·프레임) ③프롭 직경/피치 상향 또는 모터 KV 재검토 ④**H8(CG) 교정이 선행되면 임계모터 여유 +0.16을 공짜로 얻는다** — 그것부터 |

---

## 다음 세션 검증 우선순위 (지상, 비행 불필요)

> **2026-07-30 전면 재정렬.** H19(T/W 1.44)가 확정되면서 우선순위의 성격이 바뀌었다 —
> **1·2번을 하기 전에 하는 소프트웨어 튜닝은 의미가 없다.** T/W 1.44는 게인으로 고칠 수 있는
> 값이 아니고, 오늘 7비행 중 6건이 목표고도 20 m에 도달조차 못 한 이유가 그것이다.
> (종전 0번 = PID 게인 대조는 2026-07-25에 완료됐고, 1·2번 = CW/CCW 대각선 추력벤치는
> H13 기각으로 **불필요해졌다** — 대각선 축은 실재하지 않는다.)

1. **🔴 배터리 IR 실측 — 팩 단자 vs 파워모듈 양단 전압을 30 A 부하 중 동시 측정** (H1/H2 vs H3 분리, 30분)
   — 오늘 두 팩 모두 84~107 mΩ(셀당 25 mΩ, 건강한 4S의 5~10배)로 나왔다. "두 팩 다 노후"와
   "공통 배선/커넥터에 50 mΩ+"가 **같은 로그를 만들기 때문에 로그로는 영원히 못 가른다.**
   이 측정 하나가 가른다 — 팩이면 교체, 배선이면 커넥터·전선 굵기 교체.
2. **🔴 CG 저울/수평계 실측 후 배터리·페이로드를 뒤로·왼쪽으로 이동** (H8 확정, 1시간)
   — 앞/뒤 추력비 1.55배. 균등화하면 **임계 모터 여유 +0.16**. T/W를 안 건드리고 얻는 최대 이득.
   (종전 3번이었으나 H13 기각으로 "H8 vs H13 분리"가 아니라 **H8 단독 교정**으로 성격이 바뀌었다.)
3. **기체 실중량 측정 + 모터/프롭 정적 추력벤치** (H19, 반나절) — T/W 절대값 확정. 감량 또는
   프롭 피치 상향·모터 KV 재검토 없이는 20 m 순항 자체가 마진 밖이다. **모터별 개체차 확인은
   여기에 흡수** — 대각선 쌍이 아니라 4기 개별로 스윕한다.
4. **피토 영점을 현장에서·열평형 후 재캘 + 캘 직후 정지 CAS≈0 검증** (H18, 15분)
   — 영점오차의 원인은 절차가 아니라 **온도**다(1.91 Pa/°C, r²=0.956). 냉방된 차량 25 °C 에서
   캘하고 37 °C 에서 날았고, **같은 실수를 최소 두 번 반복했다.** 검증 한 줄(정지 CAS≈0)을
   넣는 것이 재캘 자체보다 중요하다. 겸해서 `VT_*` 전체를 `flight_plan.md`와 대조.
   ⚠️ **다만 중량 실측(3번) 전에는 `VT_ARSP_TRANS` 를 내리지 말 것** — 실속속도는 중량의
   제곱근에 비례하므로 `FW_AIRSPD_STALL=7`(설계중량 기준)을 믿고 낮추면 실속 위험이 커진다.
5. **`BAT1_CAPACITY` 실제값 입력 + `BAT1_R_INTERNAL`을 1번에서 잰 값으로 고정** (10분)
   — 현재 `CAPACITY=-1`(쿨롱 카운팅 불가) + `R_INTERNAL=-1`(자동추정이 0.000~0.015 Ω에 갇힘,
   참값 0.10 Ω)이라 **7비행 전부 이륙 3~5초 만에 "Emergency battery"** 오경보가 떴다.
   자동 조치는 `COM_LOW_BAT_ACT=0`이라 없었지만, **경보가 상시 울려서 f07에서 정말 위험해진
   순간을 구분할 수단이 없었다** — 이번 세션의 안전 판단을 무력화한 것이 이 설정이다.
6. **`MPC_THR_HOVER`를 0.5 → 0.69로** (H15, 5분) — HTE 실측값. 오늘 7비행 중 HTE가 valid가 된
   것은 f02 하나뿐이라, 나머지 6건은 전 구간을 틀린 0.5 피드포워드로 날았다.
7. **나침반 6면 재캘리브레이션 + `CAL_MAG0_PRIO` 복구** (H17, 30분) — 현재 `PRIO=0`(비활성),
   `cs_mag_hdg=0%`. GPS가 RTK급이라 이동 중 헤딩은 확보되지만 **정지 호버 yaw 드리프트는 위험**.
8. **운용 규칙: 팩 1개당 1비행 + 이륙 전 휴지 셀전압 ≥ 4.0 V 게이트** (비용 0)
   — f03~f07이 **한 팩으로 연속 5비행**이었고(누적 방전 260→1219 mAh), f07은 3.77 V/셀(SOC 약 40%)에서
   이륙해 추락했다. **f05·f06·f07은 뜨지 말았어야 한다.**
9. **이함 트랜지언트 저속촬영/테더 관찰** (H12) — 우선순위 하향. 오늘 데이터는 "이함 직후 트리거"보다
   **상시 추력부족 + CG 편향**을 훨씬 강하게 지지한다.
10. **(H14) 테더 상태에서 PX4 온보드 오토튠, roll 축부터 단독** — 위 1~3번이 끝난 뒤에.
    지금 튜닝하면 T/W 1.44·CG 편향 상태의 물리에 게인을 맞추게 되고, 기체를 고친 뒤 다시 해야 한다.


---

## 이력 (상태가 바뀐 시점만 기록)

- **2026-07-31(H18 원인 규명 + 초판 정정):** 사용자가 "피토 캘을 비행 직전 문 다 닫고 **에어컨 끈 차량 트렁크**에서 했는데 방법이 문제였나"라고 물어와 재분석. **절차는 옳았고 온도가 틀렸다.** 로그 10개의 지상정지구간(모터<0.05 & landed)에서 센서온도 vs 차압영점을 회귀하니 `raw_zero = +25.65 − 1.906·T`, **r²=0.956**(잔차 ±2.5 Pa). 저장값 `SENS_DPRES_OFF=−22.79`는 회귀선상 **25.4 °C**, 그 직전 값 −21.78은 **24.9 °C**에 대응하고, 비행 시 센서온도는 **35~38 °C**였다 → 온도차 11 °C × 1.91 Pa/°C = **−21 Pa**, 정지 CAS 오차 예측 5.9 m/s(실측 −5.9와 정확히 일치). **즉 장비는 그 순간의 진짜 영점을 정확히 잡았고, 냉방된 차량이라는 환경이 틀렸다. 두 저장값이 모두 ~25 °C에 대응하므로 같은 실수를 최소 두 번 반복한 것이다.** 추가 규명 2건: ①**비행 중 센서가 7 °C 식는다**(37.8→30.8, 프롭 기류) → 영점이 비행 중에만 +13 Pa 이동, **지상 한 점 영점은 공중에서 무효**. PX4엔 차압센서용 `TC_*`가 없다. ②신호 잡음 ±12~18 Pa(겉보기 ±4~5.5 m/s)에 로깅 1.0 Hz — **영점오차가 잡음의 1.7배뿐이라 이 피토로 12 m/s 임계 판정 자체가 무리다.** **🔴 초판 정정:** 07-30 리뷰에 쓴 "영점 또는 `VT_ARSP_TRANS`=10 둘 중 하나만 고쳤어도 천이 성공"은 **과장이었다**. 온도보정한 실제 대기속도는 **10~11 m/s**(대지속도 11.2 − EKF바람 1.1과 정합)이고 영점을 고쳐도 PX4가 봤을 값은 평균 8.0/최대 10.1 m/s라 12엔 미달, 10이어도 간신히 걸친다. **→ 천이 실패는 H18 단독이 아니라 H19(추력부족)의 또 다른 얼굴**로 재규정. ⚠️ **중량 실측 전 `VT_ARSP_TRANS` 하향 금지** — `FW_AIRSPD_STALL=7`은 설계중량 기준이고 실속속도는 중량의 제곱근에 비례한다. 조치의 무게중심도 "재캘"에서 **"캘 직후 정지 CAS≈0 검증"과 "천이 전 프리플라이트 게이트"**로 옮겼다(사람 기억에 의존하지 않는 유일한 방법). 상세: `logs/2026-07-30_review.md` §7-4~7-8.
- **2026-07-30:** **2차 예선 전 마지막 비행 세션(수동 1건 + offboard 7건) 분석 — 가설공간이 크게 정리됐다.** ①**H13 기각** — CW/CCW 대각선 축의 차이는 **−0.014(≈0)**. 07-21 "CCW쌍이 더 일한다" → 07-25 "CW쌍이 더 일한다"로 두 번 뒤집혔던 이유는 **그 축이 애초에 실재하지 않아 표본마다 부호가 흔들린 것**이었다. ②**H8 확정·정량화** — 진짜 축은 **앞/뒤(추력비 1.55배)** 주 + **좌/우(1.31배)** 부. CG가 기하중심보다 앞으로 약 11%·우로 약 7% 치우쳐 있고, 그 대가로 임계 모터 m0(0.836)가 먼저 포화해 m1(0.584)의 여유 0.42가 사장된다. 지상 조치 우선순위 2번으로 승격. ③**H19 신설(상위 원인)** — `hover_thrust=0.693`, **T/W ≈ 1.44**. 상승률이 지령 1.5 m/s 대비 0.44~0.86 m/s에 불과하고, f05·f07은 AUTO_TAKEOFF 상태에서 **무지령 강하**. f07은 14.1 m에서 침하해 **8.2 G로 접지(세션의 추락 건)**. **만충 팩이던 f03조차 0.83 m/s**라 배터리를 고쳐도 남는 제약이다. ④**H18 신설** — 피토 차압 영점이 **21 Pa** 어긋나(지상 정지 raw −42.6 Pa vs `SENS_DPRES_OFF=−21.78`) CAS가 저평가돼, 실제 11.2 m/s 도달했는데 PX4는 9.56 m/s를 보고 `VT_ARSP_TRANS=12.0` 미달 → **Quad-chute**로 천이 중단. **푸셔는 정상 복구됨**(+10.85 A, body ax +1.46 — 07-28/29 무추력 해소)이고 우리 코드도 무관(역천이 명령 없음). ⑤**H1/H2 vs H3 미해결 유지** — 두 팩 모두 **84~107 mΩ**(셀당 25 mΩ). 로그로는 못 가르니 **부하 중 단자 전압 직접 측정**이 유일한 갈림길로 남았다. ⑥**H17 악화** — `CAL_MAG0_PRIO`가 75 → **0(비활성)**, `cs_mag_hdg=0%`. 조치 ④(천이 금지)를 어기고 f02에서 천이했으나 GPS가 RTK급이라 헤딩오차 −10.4°로 무해했다. ⑦**H15 보강** — 7비행 중 HTE valid는 f02 하나뿐. ⑧**운용 문제 확정** — f03~f07이 한 팩 연속 5비행이고 f07은 SOC 40%에서 이륙했다. 상세·수치·재현명령: **`logs/2026-07-30_review.md`** (각 비행 판정은 해당 `notes.md`).
- **2026-07-27(정정):** **H17의 원인 판정을 정정** — 7/26에 "자기장 세기가 기준보다 30~35% 낮다 → 하드아이언/근처 자성체 의심"이라고 쓴 것은 **`vehicle_magnetometer`(캘리브레이션 적용 후) 값을 물리량으로 착각한 오류**였다. 사용자가 "GPS 신호선 알루미늄 호일 차폐(7/23·7/25 시행)가 원인 아니냐"고 물어와 원시값을 확인하면서 발견. `sensor_mag`(적용 전) = 0.441 G로 **물리적 자기장은 정상**이고, 저장된 offset(\|0.197\| G)·off-diag(YODIAG −0.182)가 그걸 0.344 G로 끌어내리고 있었다. 보정 후 산포가 원시보다 오히려 커진다(±10% → ±18%)는 것이 결정적 증거. **→ 자성체·전류간섭·호일차폐 가설 전부 기각, 조치는 "나침반 재캘리브레이션" 하나로 축소.** 교훈: **자기계를 논할 땐 반드시 `sensor_mag`(원시)와 `vehicle_magnetometer`(보정 후)를 구분해 볼 것** — 둘을 섞으면 소프트웨어 설정 문제를 하드웨어 문제로 오진단한다.
- **2026-07-26:** 2차 비행지점 15비행(flight05~19) 로그 회수·분석. ①**H16 종결** — 수정이 09:58 이전에 배포됐고 flight14·16이 조종사 개입 없이 전 구간 완주(경로이탈 최대 0.94/2.36 m, WP 최근접 0.10~0.64 m). ②**H17 신설·확정** — 자기계 100% 기각으로 헤딩 추정이 65° 틀렸고 그것이 flight17 VTOL 천이 이상방향의 원인. ③**H15 보강** — 회수한 7개 로그 중 `hover_thrust_estimate.valid`가 1이 된 건 flight17뿐. 나머지는 비행이 짧아 HTE 수렴 전에 끝났다 → "수렴 전 기동"이 상시 조건임이 확인됨. ④**H13/H12 재확인** — flight05·06·07·11·13·15가 모두 이륙 4~6초 만에 조종사 KILL, 개별모터 ≥0.99 포화 23~63%, 최저 셀전압 2.46 V. flight08은 최대추력 지령(thrust_z=−1.000) 상태로 4.9 m→접지. 상세: `logs/2026-07-25_review.md`.
- **2026-07-25(2차):** H16 수정 구현. `state_logic.py`에 `is_pilot_takeover()`/`path_origin_ned()`/`translate_path()` 순수함수 신설, `offboard_node`가 이륙 순간 경로 전체를 이륙지점 기준으로 평행이동하고(`waypoint_frame` 파라미터, 기본 `takeoff`), 조종사 인계 감지 시 `_State.PILOT_TAKEOVER`로 빠져 세트포인트발행을 영구 중단하도록 변경. `DONE`도 velocity-0 발행을 멈춘다. pytest 286 통과. **실기체 배포는 아직 안 했다** — 다음 세션에 SITL 회귀검증 후 RPi에 git pull + colcon build 필요.
- **2026-07-25:** 오늘 비행 로그(수동 2회·아밍체크 9회·offboard 2회) 분석. ①**H16 신설·확정** — offboard 경로가 EKF 로컬원점 절대좌표라 이륙지점과 최대 10.9 m 어긋났고, 이것이 "원하는 방향으로 안 간다"의 직접 원인. flight01은 여기서 조종사 KILL까지 감. ②**H13 방향 역전** — CCW쌍이 아니라 CW쌍(특히 motor0 단독)이 더 일한다. 3S→4S 배터리 교체로 무게·CG가 바뀐 것과 겹침, 대각선 프레이밍 재확인 필요. ③**H14 우선순위 0번 완료** — 레이트 게인은 PX4 기본값 확정, 다만 `MC_ROLL_P/PITCH_P`는 낮춰져 있었음. ④**H15 신설** — `MPC_THR_HOVER=0.5` vs 실측 호버 0.68~0.84. 상세: `logs/2026-07-25_flight01·flight04·manual/notes.md`.
- **2026-07-22:** 사용자가 "PID 튜닝을 한 번도 안 한 게 원인 아니냐"는 가설 검토를 요청 →
  H14(제어: PID 게인 미튜닝) 신설. 기존 H10(EKF 왜곡)·H11(얼로케이터 기하)이 조사 후 거의
  기각되어 "제어" 분류에 남은 미검증 후보가 사실상 없었는데, roll 오차가 27~33°로 커질 때까지
  얼로케이터가 포화되지 않았다는 기존 재분석 결과(2026-07-21 이력 참조)를 "게인 부족으로 초기
  반응이 느렸다"는 관점으로 재해석하면 강한 정황증거가 됨. H12/H13과 배타적이지 않고 보완관계
  (H13=교란원, H14=왜 못 버텼는가). 검증비용 0인 파라미터 대조를 우선순위 0번으로 추가, PX4
  온보드 오토튠(텔레메트리 무관, RPi5↔FC MAVLink 링크로 완결) 실행을 7번으로 추가. 상세는
  `docs/session_log.md` 2026-07-22 항목.
- **2026-07-21(4차):** 사용자가 "배터리 말고 다른 원인은 없나, 폭넓게 생각하라"고 요청 →
  이미 회수된 flight04·flight08·flight09의 `analyze_flight.py` 자동분석에 있던 "모터별
  커맨드" 항목을 축별로 다시 비교. **H13 신설·최유력으로 등재:** 세 비행(서로 다른 배터리
  팩 사용) 모두 CW 대각선(motor0 전우+motor1 후좌) 평균 0.45~0.51 vs CCW 대각선(motor2
  전좌+motor3 후우) 평균 0.67~0.76로 동일방향·유사배수 재현, `imbalanced_prop_metric`도
  세 비행 동일부호. 배터리 팩이 달라도 재현되므로 배터리보다 이 기계적 비대칭이 고전류·
  고도미달의 더 유력한 설명. H5(좌/우 프레이밍)는 부정확했던 것으로 정정, H13으로 흡수.
  H12는 "H13 발견 전 최유력"으로 하향 표기(기각 아님, H13과 공존 가능).
- **2026-07-21(3차, 정정):** 직전 항목("2차")에서 flight09를 "신품 배터리 교체 후 재비행"으로
  해석해 H1/H2를 반증·H3을 상향했으나, **사용자 확인 결과 이 "교체 배터리"는 신품이 아니라
  원래 보유한 두 팩 중 나머지 하나(둘 다 기존 사용 이력 있음)** — 신품 대조군 실험이 아니었음.
  **H1/H2/H3 상태를 정정 이전으로 되돌림**(가설 테이블 참조). flight09 자체의 비행 관측
  (목표고도 미달, 배터리 페일세이프 재진입, 지상에서 무입력 roll 요동)은 사실 그대로 유효 —
  다만 "배터리 교체로도 안 고쳐졌다"는 인과적 결론만 철회.
- **2026-07-21(2차, 정정됨— 위 3차 참조):** 사용자가 배터리를 두 번째 팩으로 교체하고 즉시
  재비행(flight09). 결과: 휴지전압 12.36V(정상)인데도 부하중 8.61V/39.3A까지 붕괴 —
  flight04(첫 번째 팩)와 동급 이상으로 재현됨. 이 비행은 목표고도 4.0m에 근접도 못함(최대
  dist_bottom 0.99m), 배터리 페일세이프로 AUTO_LOITER 강제진입 재현(flight04와 동일 패턴),
  이함 실패 후 지상(dist_bottom=0.00m)에서 고전류(20~33A) 지속 중 스틱입력 없이 roll
  -17.5°까지 요동(t=26.56s) 후 조종사 POSCTL 개입으로 착지. 상세:
  `logs/2026-07-21_flight09/notes.md`.
- **2026-07-21:** H12를 최유력으로 승격. `torque_setpoint_achieved`가 roll/pitch/yaw/thrust
  통합 플래그라 roll 전용 판단엔 부적합했음을 확인 → `unallocated_torque[0]`로 재검증해
  "얼로케이터 포화=원인" 프레이밍을 정정(결과일 뿐)하고 진짜 트리거를 이함+1.3초 시점(t≈3.6s)으로
  재특정. 모드전환 출처(조종기 vs 페일세이프)도 `nav_state_user_intention`+`failsafe`로
  재확인해 두 비행 모두 최초 폭주는 순수 소프트웨어 제어 구간에서 일어났음을 재확인.
  상세: `logs/2026-07-20_flight02·flight03/notes.md`, `docs/session_log.md` 2026-07-21.
- **2026-07-20:** 최초 가설 12개(H1~H12) 수립. flight02/flight03/수동비행 5건 최초 pyulog 분석
  기반. 상세: `logs/2026-07-20_flight02·flight03·manual/notes.md`, `docs/session_status.md`
  🚁 mc-실기체 트랙.
