---
doc_type: handoff_plan
project: suridoksuri-1
track: 🛩 sitl-vtol → 🔧 main-code / ✈ vtol-실기체
scope: SITL-7 후속 — 저위험 안전조치 우선 + AUTO_PATH/A안 조건부 판정 + 결함 전체 해소 (R1~R7)
created: 2026-07-27
updated: 2026-07-27 (재검토 — A안을 "채택 확정"에서 "조건부 판정"으로, 마감 우선순위 반영)
status: 계획 확정, 실행 대기
---

# SITL-7 후속 개선 계획 — 인수인계

> **이 문서 하나로 다음 오케스트레이션 세션을 시작할 수 있게 쓴 것이다.**
> §1(확정 사실)은 **재조사 금지** — 전부 실측·소스 검증을 마친 것이다.
> §3의 R1~R7을 순서대로 굴린다. **R1·R2는 제어법칙을 바꾸지 않는 마감 전 조치이고,
> A안은 R3에서 실측으로 판정한 뒤 채택된 경우에만 R4에서 구현한다.**
> §4는 굴리기 전에 받아둘 결정 — **2·3·4번이 R1·R2를 막는다.**

---

## 0. 목표와 우선순위 (2026-07-27 재검토로 갱신)

SITL-7 캠페인(24런)이 도출한 **결함 16건**을 해소한다.

> ### ⚠ 초판(같은 날 앞서 작성)에서 바뀐 것 — A안은 "채택 확정"이 아니라 "조건부"다
>
> 초판은 **A안(`setpoint_raw/local` 위치+속도 → `FW_POSCTRL_MODE_AUTO_PATH`)을 R1~R3에 놓고
> "권고: 채택"** 으로 썼다. 재검토 결과 그 판단은 **"버그를 없앤다"에 끌린 것이었고,
> 정작 실기체 빌드(`c890d9db0a`)는 그 버그의 영향을 받지 않는다는 점을 충분히 반영하지 않았다.**
>
> - 어차피 **PX4는 핀으로 고정**한다. 업그레이드가 필요해지면 상류 한 줄 패치
>   (`_pos_sp_triplet.current.course = NAN;`)가 제어법칙 교체보다 훨씬 작은 변경이다.
> - 가드 있는 빌드에서는 **현행 위치-only 방식이 이미 작동한다**(A1 cte 1.1m / A3 7.2m,
>   전 경로 완주, cte 마지막 1m 이하 수렴).
> - A안의 실측 근거 **0.2m 는 PX4 내부 `track_err`(직선 한 레그, 55초)** 이고,
>   우리 캠페인 cte(폴리라인 기준, 천이 진입 포함)와 **비교 불가**다.
>   → **A안의 증분 이득은 한 번도 측정된 적이 없다.**
>
> **그러므로 A안은 실측 비교로 판정한다**(§2-1의 채택/기각 기준). 판정 전에는 구현하지 않는다.

### 마감(2차예선) 고려 — 급격한 전체 수정을 피한다

**제어법칙을 바꾸지 않는 저위험 안전 조치를 먼저 확보하고 배포한다.**
A안은 제어법칙 교체이므로 **판정 실험 → 결정 → (채택 시에만) 구현** 순으로 뒤로 뺀다.
마감이 닥치면 R1·R2까지만 반영된 상태로도 **지금보다 명확히 안전한 기체**가 된다.

| 우선순위 | 내용 | 제어법칙 변경 | 마감 전 필요 |
|---|---|---|---|
| **1** | 타임아웃 4종 + 거리 상한 + `_step_hold` 슬루레이트 + 진단로그 2건 | **없음** | **예** |
| **2** | `_find_segment` 전역탐색 → 캐시 활용 (폐회로 잠재 위험) | 없음(버그 수정) | **예(경로가 폐회로면)** |
| 3 | A안 판정 실험 (프로브만, 비행코드 무수정) | 없음 | 아니오 |
| 4 | A안 구현 — **판정에서 채택된 경우에만** | **있음** | 아니오 |
| 5 | 경로·고도(`_cruise_alt`, lookahead 적응, `d_end_thresh`) | 부분 | 아니오 |
| 6 | 선회 품질·플래너 블로킹·미실측 보강 | 부분 | 아니오 |

**A안이 해결하는 것:** PX4 버전 의존(F-4) 제거 + (미입증) 바람·단거리 추종 개선.
**A안이 해결하지 못하는 것:** 나머지 15건 전부. 특히 **F-5(선회 중 고도 침하)는 종방향(TECS)
문제로 보여 횡방향 유도를 바꿔도 달라질 이유가 없다.** 경로 품질(NR 잔차 곡선 변형,
`_cruise_alt` 스칼라화, 플래너 블로킹)도 전부 우리 몫이다.

---

## 1. 인계 시점의 확정 사실 (재조사 금지)

### 1-1. 환경

| 항목 | 값 |
|---|---|
| SITL 호스트 | WSL 배포판 `Ubuntu-22.04` (E드라이브). `wsl.exe -d Ubuntu-22.04 -- bash -lc '...'` |
| **실기체 PX4 빌드** | `/root/PX4-vehicle` (worktree, `c890d9db0a` = 실기체 탑재본, course 가드 **있음**) |
| 기존 SITL 빌드 | `/root/PX4-Autopilot` (`9bb0d365c4`, 태그 `sitl7-orig-head`, 가드 **없음**) |
| 런 실행 | `wsl.exe -d Ubuntu-22.04 -- bash /root/s4/run_vehicle.sh <ID> --run-id <ID>_<tag>` |
| 워크스페이스 | `/root/drone_ws`, 저장소 클론 `/root/drone_ws/src/suridoksuri` |
| MAVROS | `udp://:14540@localhost:14580` |
| 하니스 | `tools/sitl/` — `run_scenario.py`(주입훅·RangeGuard) / `analyze_run.py` / `path_geometry.py` / `scenarios.yaml` |

### 1-2. PX4 상류 회귀 (근본원인, 전문은 `sitl_vtol_fw_offboard_rootcause.md`)

`FixedWingModeManager.cpp`의 오프보드 분기가 `_pos_sp_triplet = {}`로 제로초기화 후
`cruising_speed/vx/vy/vz/lat/lon/alt/gliding_enabled`에만 NaN을 넣는다. **`course`가 빠졌다.**
`msg/PositionSetpoint.msg:36` 규약이 `NaN = unused`이므로 **0.0f = "코스 0 rad(정북) 유지" 유효명령**이 되고,
그 분기의 경로 기준점이 기체 현재 위치라 **횡오차가 구조적으로 항상 0 → 경보·페일세이프가 안 걸린다.**

- 가드 `2e59c98b7c`(05-27) → revert `1499238f1c`(07-03)
- **실기체 `c890d9db0a` = 가드 O, revert X → 안전.** SITL 기존 `9bb0d365c4` = 취약.
- **실기체 PX4 업그레이드 금지** (A안 선행 없이는).
- **실기체 FW+OFFBOARD 비행 실적 0건** (ulog 78건 전수 스캔). 이 캠페인 전체가 SITL 근거다.

### 1-3. A안 실측 근거 (S3 프로브, `logs/2026-07-27_s3_fw_offboard_probe/`)

목표 정동 800m, 각 페이즈 ~55초:

| 페이즈 | 결과 |
|---|---|
| **위치+속도** | dE +794.6m, 지상코스 90.0°, **횡오차 0.2m** — `FW_POSCTRL_MODE_AUTO_PATH` 진입 |
| 위치만 (현행) | dN +814.7m, 코스 359.9° — 버그 발현 |
| 속도만 | `FW_POSCTRL_MODE_OTHER`로 빠져 **setpoint 발행 자체가 52.96초 정지** — 기각 |

**A안 구현 규약 (소스 근거 확정분):**
1. `/mavros/setpoint_raw/local` (`mavros_msgs/msg/PositionTarget`)
2. `coordinate_frame = FRAME_LOCAL_NED`(=1), **값은 ENU로 채운다**(MAVROS가 NED 변환 — 현행 PoseStamped와 동일 규약)
3. `type_mask = 3520` = `IGNORE_AFX|AFY|AFZ|YAW|YAW_RATE` (64+128+256+1024+2048). **위치·속도 활성**
4. `velocity`는 `velocity_2d.normalized()`만 쓰인다 → **크기는 무시, 방향만 유효**
5. **가속도는 반드시 마스크로 무시** — finite면 곡률(`loiter_radius`)을 계산해 선회를 걸어버린다
6. `cruising_speed`는 PX4가 무조건 NaN으로 덮는다 → **오프보드에서 순항속도 지정 불가.**
   대기속도는 `FW_AIRSPD_TRIM`으로만 정해진다. **`v_cruise`는 플래너 경로 형상에만 영향**
7. **MC 구간은 현행 위치-only 유지**(`mc_pos_control`은 `course`와 무관). MC/FW가 채널을 공유하면 `type_mask`를 구간별로 달리 줄 것

### 1-4. 캠페인 실측 기준선 (A안 도입 후 재측정 대상)

실기체 PX4 빌드 기준. **A안 적용 후 이 표와 나란히 비교해야 개선/악화를 판정할 수 있다.**

| 지표 | 현행 실측 |
|---|---|
| FW cte 최대 → 마지막 | A3 7.2→0.3 / B3 7.0→0.1 / B4 13.9→0.9 / B5 8.6→1.0 / C2 19.6→0.1 m |
| 코너 오버슈트 / WP 최근접 | 90°: 0.085~0.915m / 19~22m · 135°: 4.79m / 37.6m |
| **선회 중 고도 침하** | 직선 2.16 / 28° 1.95 / 90° 4.73 / 폐곡선 6.82 / **135° 7.45** / **직선인데 C2 6.76** m |
| setpoint 계단 | TRANSITION_FW→STREAMING **214~300m** · TRANSITION_MC→HOLD **60~117m** |
| 정천이 / 역천이 | 2.42~2.60s / 4.92~6.09s |
| 역천이 종점 통과량 | ≈ `57 − d_end_thresh` (선형). 현재 기본 10 → **+46.8m 통과** |
| 헤딩 정렬 잔류오차 | 회전량 비례: 4° 회전→0.4° / 95° 회전→2.4~2.5° (허용치 2.86°) |
| 바람 8m/s 영향 | cte 1.2→4.0m, 고도편차 2.24→3.99m, **정렬 허용대 재이탈 0→12.5°** |
| 제어틱 p95 | 104ms (N=301·501 동일). `_find_segment` O(N) 우려는 **기각됨** |
| 플래너 `__init__` 블로킹 | 2WP 즉시 / L자 50~69s / U턴 45~73s / **폐곡선 263.5s**. `v_cruise`에 160배 민감 |

### 1-5. 검증된 안전 경로 (건드리면 회귀 확인 필수)

- **OVERRIDE(FW)** MANUAL 거부 → 10틱 후 AUTO.LOITER 폴백 → DONE. 스트림 정지
- **OVERRIDE(MC)** POSCTL 분기 (S7 최초 검증)
- **PILOT_TAKEOVER** POSCTL 실제 진입 → 인계 감지 1회 → 스트림 영구 정지 → `vehicle_command` 0건
- **OFFBOARD 강제 이탈 복구** 상실 0.97s 후 복구, 완주. mode flapping 없음
- **C8 home_amsl/geoid** `alt=53.0m AMSL (지면 3.0+50.0)` 정확 — 작업 H-2 VTOL 이관 합격

---

## 2. 결함 → 작업 패키지

| WP | 대상 결함 | 내용 |
|---|---|---|
| **WP-A** | F-4 | **A안 도입** — FW 구간 `setpoint_raw/local` 위치+속도 |
| **WP-B** | F-1, F-2, F-3 | **타임아웃 + 거리 상한** — `TRANSITION_FW`/`TRANSITION_MC`/`ENTRY`/`CLIMBING`, 이륙지점 기준 거리 감시 |
| **WP-C** | F-6, F-9 | **경계 부드러움** — `_step_hold` 슬루레이트, 천이 고도 계단 램프, TRANSITION_FW→STREAMING 스냅백 |
| **WP-D** | F-8, F-10, F-11 | **경로·고도** — `_cruise_alt` 스칼라화 해소, 짧은 경로 `_FW_LOOKAHEAD` 적응, `d_end_thresh` 기본값 |
| **WP-E** | F-5, F-7 | **선회 품질** — 선회 중 고도 침하, 헤딩 정렬 마진 |
| **WP-F** | F-13, F-14, F-16 | **정리·진단** — 미사용 속도 프로파일, ENTRY의 FOLLOWING 오로그, 재요청 WARN 10Hz |
| **WP-G** | F-12 + 미실측 | **플래너 블로킹**, F-15(정렬구간 OFFBOARD 이탈)·`param_set` 주입·C9 |
| **WP-H** | — | **실기체 배포·검증** |

---

## 2-1. A안 — 기대이득·리스크·채택 기준

### 기대이득 (가설, 미입증)

현행과 A안은 **PX4에 무엇을 주느냐**가 다르다. 둘 다 NPFG를 쓰지만:

- **현행(위치만)** → PX4에 ***점*** 을 준다. 그 점을 조준해 난다.
  횡오차 규제는 **전적으로 우리 lookahead 기하학**이 정한다(pure-pursuit).
  의도한 경로 대비 정상상태 오프셋을 **아무도 규제하지 않는다.**
- **A안(위치+속도)** → PX4에 ***선***(점 + 접선)을 준다. PX4가 **그 트랙에 대해 직접 횡오차를 규제**한다.

**기대 근거 (실측):**
- C4에서 바람 8m/s에 **cte 1.2 → 4.0m (3배 악화)**. 점 추적의 전형적 서명 —
  밀린 만큼이 그대로 오프셋으로 남는다. 트랙 규제로 넘기면 줄어야 한다.
- B7에서 `_FW_LOOKAHEAD`(70m) > 경로 전장(40m)이라 목표가 항상 종점에 클램프되어
  **추종 구간이 0.9초로 소멸**. 트랙 규제는 lookahead 의존이 약해 구조적으로 완화될 수 있다.

**경로 생성에는 아무 영향이 없다.** A안은 추종만 바꾼다.

### 리스크 (현행에 없던 불안정 경로가 새로 생긴다)

| # | 리스크 | 근거 |
|---|---|---|
| **R-a** | **모드 토글 채터링.** `AUTO_PATH` 진입 조건은 매 업데이트 `ISFINITE(lat&&lon&&vx&&vy)`. **속도가 한 틱이라도 NaN이면 `AUTO`(점 추적)로 강등** → 10Hz로 서로 다른 유도법칙을 오간다. 가드 없는 빌드에선 강등 = 즉시 폭주 | `FixedWingModeManager.cpp:389-392` |
| **R-b** | **방향 지령의 노이즈 민감성 + 접선 반전.** 점 대신 *방향*(미분량)을 지령한다. `_find_segment`가 **캐시를 무시하고 매 틱 전역 최근접 탐색**을 하므로, **폐곡선·U턴에서 반대 레그를 잡으면 접선이 통째로 뒤집힌다.** 점 추적에선 목표점이 옮겨갈 뿐(B5·B4 무사 완주)이지만 선 추적에선 **지령 트랙이 반전**된다. **대회 경로가 폐회로면 정확히 이 조건** | `l1_guidance.py:183-208`, 정적 감사 E-12 |
| **R-c** | **종점 거동 악화.** 선에는 끝이 없다 → 종료 판정 실패 시 궤도가 아니라 **무한 직선 이탈**. 현행은 점 주위를 돈다(C1b에서 실제로 2바퀴 돌고 종료). B7은 이미 종점을 73m 지나친다 | C1b, B7 |
| **R-d** | **NaN 기본값 함정 — 이번 사고와 같은 계열.** `PositionTarget`의 가속도 기본값은 **0.0이지 NaN이 아니다.** `type_mask` 상수 하나가 유일한 방어선이고, 틀리면 0.0이 유효 지령이 되어 PX4가 곡률을 계산해 선회를 건다. `course=0.0f`가 "정북 유지"로 읽힌 것과 **완전히 같은 함정을 우리 쪽에 들여오는 것** | `FixedWingModeManager.cpp:2139-2151` |
| **R-e** | **yaw 무시와 천이의 상호작용.** `type_mask=3520`은 yaw를 무시하는데 `TRANSITION_FW` P3는 **MC 상태에서 시작**한다. 13~15초 들여 맞춘 헤딩이 천이 진입 방향을 정하는데 그 구간에서 yaw 지령이 사라진다 | §1-3 |
| **R-f** | **장기적으로 업그레이드에 더 취약할 수 있다.** `course`/`AUTO_PATH`는 **지금 upstream이 활발히 흔들고 있는 영역**(가드 05-27 도입 → 07-03 revert). "점으로 날아가기"는 오래되고 다져진 경로이며, 그게 깨진 유일한 이유가 그 새 기능이 새어 들어온 것이다 | §1-2 |

### 채택 / 기각 기준 (R3에서 **사전 확정 후** 실험할 것)

**채택** — 아래 셋을 모두 만족:
1. **바람에서 유의한 개선**: C4 동일 조건(8m/s)에서 cte 최대가 현행 4.0m 대비 **의미 있게 감소**
2. **접선 반전 미관측**: B5(폐곡선)·B4(U턴)에서 지령 트랙 반전이 **0건**
3. **모드 이탈 0건**: 전 구간 `AUTO_PATH` 유지, `fixed_wing_lateral_setpoint` 발행 공백 0

**기각** — 하나라도 해당:
- 바람에서 개선이 없거나 악화
- 폐곡선·U턴에서 접선 반전 관측 (R-b 실현)
- 모드 이탈 1건 이상 (R-a 실현)

**기각 시 조치:** 현행 유지 + **PX4 핀 문서화**. 업그레이드가 필요해지면 상류 한 줄 패치
(`course = NAN`)로 대응한다 — 제어법칙 교체보다 훨씬 작고 되돌리기 쉽다.

---

## 3. 세션 계획 R1~R7

> 공통 금지: **SITL 실행 세션은 항상 1개만**(gz 서버·UDP 14540/14580 점유).
> 공통 규율: 커밋 태그 `[sitl]`(코드 수정 포함 시 `[main]` 병기), push는 오케스트레이터가 관리.
> **모든 세션은 실기체 PX4 빌드(`/root/PX4-vehicle`)를 기본으로 쓴다.**

### R1 — 저위험 안전 조치 ① 타임아웃 + 거리 상한 (**마감 전 필수**)

**대상:** F-1(ENTRY 무한대기 5.85km 실측) · F-2(역천이 무한) · F-3(정천이 무한) · F-15(정렬구간 이탈)
**제어법칙을 바꾸지 않는다.** 실패했을 때 어디로 떨어지는지만 정의하는 작업이다.

**설계 (§4 결정 2·3 확정 후):**
- `TRANSITION_FW`/`TRANSITION_MC`/`ENTRY`/`CLIMBING`에 타임아웃 파라미터 신설.
  **규약은 `hold_timeout`·`mc_wp_timeout`과 동일.** 초과 시 강제 진행이 아니라 **안전 폴백**.
- 폴백 목적지: **이미 검증된 `_request_override()` 재사용**(manual 시도 → AUTO.LOITER).
  **새 경로를 만들지 말 것** — S7에서 안전경로 3종이 실증됐다. 새로 만들면 그것부터 다시 검증해야 한다.
- **거리 상한**: 이륙지점 기준 수평거리가 파라미터 초과 시 OVERRIDE.
  하니스 `RangeGuard`가 C7에서 1564m에 실제 발동해 7km 비행을 막은 전례가 있다 — **같은 감시를 노드에 넣는다.**
- **F-15**: 정렬 구간에서 OFFBOARD를 잃으면 `_fw_offboard_requested`가 이미 True라 재요청하지 않는다.
  재요청 경로를 열되 **조종사 인계 가드(`is_pilot_takeover`)를 반드시 통과**시킬 것.

**검증 (주입):** C10 재현(ENTRY 무한대기 → 타임아웃에 걸리는가) · 정렬구간 OFFBOARD 이탈 주입(F-15) ·
거리 상한 발동 · **기존 안전경로 3종 회귀**(OVERRIDE-FW/MC, PILOT_TAKEOVER) · A1/A3 완주 회귀.
주입은 `on_log` 트리거 + in-process rclpy로(CLI는 4.04초 지연이라 짧은 이벤트를 못 노린다).

**게이트:** 오케스트레이터가 ①C10이 이제 타임아웃에 걸려 안전 상태로 떨어지는지 ②안전경로 3종이
그대로인지 ③정상 미션이 타임아웃에 걸리지 않는지(오발동 없음)를 직접 확인.
**종료 시 실기체 배포**(R7 절차) — 마감 전 확보해야 할 실질 안전 이득이다.

---

### R2 — 저위험 안전 조치 ② 추종 견고성 + 진단 (**마감 전 필수, 경로가 폐회로면 특히**)

**전부 제어법칙 불변. 버그 수정과 로그 정정이다.**

1. **`_find_segment` 전역 최근접 탐색 → 캐시 활용** (`l1_guidance.py:183-208`).
   현재 캐시 `_seg_idx`를 갱신만 하고 탐색엔 쓰지 않아 **매 틱 전역 스캔**을 한다.
   지금은 무해하지만 **폐회로·U턴처럼 레그가 서로 가까이 지나가면 엉뚱한 레그를 잡을 수 있다.**
   **A안 채택 여부와 무관하게 먼저 고칠 것** — A안에서는 이게 직접적인 불안정 원인(R-b)이 된다.
   수정 방향: 이전 인덱스 근방 윈도 탐색 + 역행 금지. **회귀 위험이 있으므로 B5·B4·B3로 검증 필수.**
2. **`_step_hold` 슬루레이트** (F-6, 2026-07-21 수정후보 ②로 남아 있던 것).
   MC 분기의 `_mc_pos_ramp`와 같은 방식. HOLD 계단 60~117m를 완화한다.
3. **진단 로그 2건**
   - F-14: `entry_mode=mid_flight`에서도 `OFFBOARD 확인 → FOLLOWING`을 찍는다 → 실제 전이 상태를 찍을 것
   - F-16: OFFBOARD 재요청 WARN이 **throttle 밖에서 10Hz**로 찍힌다(실제 명령은 1Hz 1건).
     **로그만 보면 2026-07-25 조종사 인계 사고와 육안 구별이 안 된다** → 로그도 throttle 안으로
4. **플래너 블로킹 완화 (최소 조치)** (F-12) — `__init__` 동기 실행으로 폐곡선 263.5초가 걸린다.
   **비동기화는 하지 말고**(범위가 크다) 진행 로그 + 예상시간 출력만 넣는다.
   현장에서 "launch가 죽었나" 싶어 재기동하면 중복 인스턴스가 된다.
   함께: **`v_cruise` 정식값 결정**(§4 결정 4) — 이 값이 플래너 계산시간을 160배 좌우한다.

**검증:** A1·A3·B3·B4·B5(폐곡선, `_find_segment` 회귀 핵심)·B7 + `_step_hold` 계단 재측정.
**게이트:** 오케스트레이터가 `_find_segment` 수정이 **B5·B4에서 세그먼트 오선택을 실제로 줄이는지**
(수정 전후 세그먼트 인덱스 시계열 비교)를 직접 확인. **"고쳤다"는 자기보고로 통과시키지 말 것.**
**종료 시 실기체 배포.**

---

### R3 — A안 판정 실험 (**비행 코드 무수정**, 프로브만)

**여기서 A안의 채택/기각을 결정한다.** §2-1의 기준을 **실험 전에 확정**하고 시작할 것.
전체 미션 통합 없이 **프로브 스크립트로 판정 데이터를 뽑는 것이 핵심** — 마감 부담을 최소화한다.
S3의 프로브(`logs/2026-07-27_s3_fw_offboard_probe/`, `tools/sitl/fw_offboard_probe.py`)를 확장한다.

**(a) 소스 조사 — 반드시 먼저 (PX4 `control_auto_path()` 정독):**
1. **`velocity` 방향의 의미** — 정의되는 경로가 "position setpoint를 지나고 velocity 방향을 갖는
   직선"인가? 그렇다면 우리가 넣을 방향은 **기체→목표점 베어링(`chi_cmd`)이 아니라 경로 접선**이다.
   cte가 클 때 둘은 크게 다르다. **확정 없이 구현 금지.**
2. **모드 이탈 (R-a)** — velocity가 한 틱 NaN이면 `FW_POSCTRL_MODE_AUTO`로 떨어지는가?
   **떨어진다면 발행 누락 한 틱이 폭주로 이어진다. A안 최대 리스크.**
3. **종점 처리 (R-c)** — 경로 끝에서 (점, 접선)을 계속 주면 지나쳐 직진하는가, loiter로 도는가?
4. **NaN 마스크 (R-d)** — `type_mask`가 틀렸을 때 accel 기본값 0.0이 실제로 곡률 지령이 되는가?
5. **고도(`position.z`) 추종** — AUTO_PATH가 z를 어떻게 쓰는가 (F-5·F-10 재측정 기준)
6. **yaw 무시 (R-e)** — `TRANSITION_FW` P3는 MC 상태에서 시작한다. 그 구간에서 yaw 무시가 안전한가?
7. 가드 있는 빌드(`c890d9db0a`)에서도 `:389-392`가 동일한지 소스 확인

**(b) head-to-head 프로브 — 같은 가드 빌드에서 A안 vs 현행:**

| 실험 | 목적 | 판정 |
|---|---|---|
| **바람 8m/s 직선** | **채택 기준 1.** 현행 cte 4.0m(C4) 대비 | 의미 있게 감소하는가 |
| **폐곡선 / U턴 접선** | **채택 기준 2.** R-b 검증 | 지령 트랙 반전이 0건인가 |
| **단거리 40m** | B7의 추종 구간 소멸이 완화되는가 | 추종 구간이 생기는가 |
| **종점 통과** | R-c 검증 | 무한 직선 이탈인가 |
| **모드 유지** | **채택 기준 3.** R-a 검증 | `AUTO_PATH` 유지, `fixed_wing_lateral_setpoint` 공백 0 |

**산출물:** `docs/sitl_vtol_auto_path_spec.md` — (a) 7개 답(소스 줄번호 인용) + (b) 실측표 +
**채택/기각 판정과 그 근거**.
**게이트:** 오케스트레이터가 (a)2번과 (b)의 채택기준 3개를 직접 재현 확인.
**금지:** 비행 코드 수정. 판정 전 구현 금지.

---

### R4 — A안 구현 + 검증 (**R3에서 채택된 경우에만**)

**기각됐으면 이 세션은 통째로 건너뛴다.** 현행 유지 + PX4 핀 문서화로 끝낸다.

**구현:**
1. `fc_ros/fc_ros/adapters/` — `PositionTarget` 발행 어댑터 신설(현행 `SetpointPublisher`는
   `TwistStamped` 전용이라 그대로 두고 별도 클래스). ENU 변환·`type_mask`·`coordinate_frame`을
   한 곳에 가둘 것. **가속도는 마스크로 무시 + 값도 명시 NaN**(R-d 이중 방어).
2. `fc_bridge/utils/` 에 **순수 함수**로 분리: 경로 접선 산출, NED→ENU 변환, `type_mask` 상수.
   rclpy 없이 테스트 가능해야 한다(이 저장소 관례).
3. `offboard_node.py` — **FW 구간만** 교체. R3-(a)6의 답에 따라 `TRANSITION_FW` P3 처리를 결정.
   MC 구간(STREAMING-MC / FOLLOWING-MC / HOLD)은 **현행 위치+yaw 유지**(§4 결정 1).
4. **velocity 발행 누락 방지** — R3-(a)2가 "한 틱 NaN이면 강등"이면, velocity가 유효하지 않은
   틱에는 **아예 발행하지 않는** 편이 안전하다(스트림 gap 1초 미만이면 `COM_OF_LOSS_T` 미달).
5. `v_cruise`를 setpoint로 전달하는 코드가 있으면 정리 — **오프보드에서 순항속도 지정 불가**(§1-3 6).

**단위 테스트:** 접선 방향(직선/코너/종점), ENU 변환 왕복, `type_mask` 값, NaN 보장.
`test_rotation.py`의 왕복검증 방식을 따를 것.

**SITL 검증 — 두 PX4 빌드 양쪽 통과가 합격 기준.** A안의 존재 이유가 "가드 유무 무관"이므로
**취약 빌드(`sitl7-orig-head`)에서도 통과해야 성립한다.**
시나리오: A1 · A3 · B3 · B4 · B5 · B7 · C2 · B8 · C4 (실기체 빌드) + A3 · C2 (취약 빌드).
§1-4 기준선 표 전 항목 재측정 → **개선/악화/불변**을 표로.

**게이트:** 오케스트레이터가 ①취약 빌드 통과 ②모드 이탈 0건을 ulog로 직접 확인 +
**단위 테스트를 일부러 깨뜨려 red 확인 → 원복 green 재확인.**
**중단 조건:** 취약 빌드에서 폭주 재현 시 A안 전제가 깨진 것 — 즉시 중단·보고.
**종료 시 실기체 배포.**

---

### R5 — 경로·고도 (WP-D) — 마감 후

**A안 채택 시 R4 결과를, 기각 시 §1-4를 기준선으로 삼는다.**

1. **`_cruise_alt` 스칼라화 해소** (F-8) — 플래너가 이미 만드는 `alt_arr`/`gamma_ref`를 쓴다.
   A4로 실증됨(중간 WP z=80m가 완전히 버려짐).
2. **천이 고도 계단** (F-9) — `transition_alt ≠ waypoints[-1].z`일 때 첫 틱 ±30/−70m 계단. 램프로 완화.
3. **짧은 경로 `_FW_LOOKAHEAD` 적응** (F-11) — 70m 고정이 40m 경로에서 추종 구간을 없앤다.
   경로 전장에 대한 상한을 두는 정도로.
4. **`d_end_thresh` 기본값** (§4 결정 5) — 통과량 ≈ `57 − thresh`.
5. **TRANSITION_FW→STREAMING 스냅백** (F-6) — 남아 있으면 목표점 산출을 일치시킨다.

**검증:** A1·A4·B7·C1a·C1b + 기준선 대비 악화 항목 재측정.

---

### R6 — 선회 품질·정리·최종 회귀 (WP-E + WP-F + WP-G) — 마감 후

1. **선회 중 고도 침하** (F-5) — **A안으로는 안 고쳐질 가능성이 높다**(종방향 TECS 문제 추정).
   현재 현상과 용량-반응만 확인됨. 침하는 코너 급격도가 아니라 **실제 선회량**에 붙는다(C2가 근거).
   원인 규명부터.
2. **헤딩 정렬 마진** (F-7) — `wp0_heading_tol` 확대 또는 정착 조건 개선.
   근거: 잔류오차가 허용대 폭에 붙어 있고, 바람 8m/s에서 재이탈 12.5°.
3. **미사용 속도 프로파일 정리** (F-13) — `v_terminal`/`decel_dist`/`apply_terminal_decel`.
   **오프보드에서 대기속도를 지정할 수 없다**는 사실을 문서에 못박을 것.
4. **플래너 비동기화** (F-12 근본 조치, R2에서 로그만 넣었다면).
5. **미실측 보강** — `param_set` 주입 경로, C9.
6. **최종 전 시나리오 회귀** — `scenarios.yaml` 26런 전량, 실기체 빌드.

---

### R7 — 실기체 배포 (WP-H) — **R1·R2·R4 각각의 종료 절차로 수행**

**CLAUDE.md 규칙: FC 코드를 고쳤으면 즉시 실기체까지 반영한다 — 묻지 말고.**
유일한 예외는 사용자가 "현재 비행중"이라고 말한 경우.

절차는 `docs/rpi_deploy.md`. 요지:
`git push` → RPi `git pull` → 컨테이너 안에서 `colcon build --packages-select fc_ros` → 검증.
- **stale colcon build가 실비행 8건의 근본원인이었다**(`4dc30f9`) — 소스↔install md5 대조 필수
- 컨테이너 `fc`가 꺼져 있으면 `docker start fc` 선행
- 미커밋 로그 폴더가 `git pull`을 막으면 `rpi_deploy.md` §3 백업 절차
- **RPi5 fc_bridge venv numpy 버전 확인**(2.x면 `np.trapz` 계열 이슈 이력)
- **PX4는 절대 업그레이드하지 않는다** (§1-2)

**실기체 검증 계획도 함께 세울 것** — 실기체 FW+OFFBOARD 실적이 **0건**이므로
첫 비행은 보수적으로: 짧은 **비-정북** 직선 레그, 조종사 즉시 인계 대기, 거리 상한 작게.

---

## 4. 사용자 결정 — 2026-07-27 확정분

> **결정 2·3·4는 2026-07-27 오케스트레이션 세션에서 사용자가 확정했다. 재질의 금지.**
> 결정 1·5는 A안 판정(R3) 이후에 필요하므로 아직 열려 있다.

| # | 필요 시점 | 결정 | **확정값 (2026-07-27)** |
|---|---|---|---|
| **2** | **R1 (마감 전)** | 타임아웃 초과 시 어디로 갈 것인가 | ✅ **`_request_override()` 재사용** (manual 시도 → AUTO.LOITER 폴백). S7에서 실증된 안전경로. **새 폴백 경로 신설 금지** — 만들면 그것부터 다시 검증해야 한다 |
| **3** | **R1 (마감 전)** | 거리 상한 기본값 | ✅ **300m** — 사용자 확정. 파라미터화하되 **yaml 기본값을 300.0으로 켜서 배포한다**(끄고 배포하면 실기체에 아무 효과가 없다). 현장 조정은 launch 인자로 |
| **4** | **R2 (마감 전)** | yaml `v_cruise: 20.0`("임시" 주석)의 정식값 | ⏳ **사용자가 `FW_AIRSPD_TRIM` 실값을 모른다 → R2 세션이 RPi5 MAVROS(`/mavros/param/get`)로 실기체 파라미터를 직접 읽어와 보고할 것.** 확인 전까지 **`v_cruise=20.0` 유지**(낮추면 기체가 못 도는 선회를 플래너가 계획하므로 안전측이 아니다). 실값 확인 후 그 값에 맞춰 확정 |
| — | **R2 (마감 전)** | **대회 경로 형상** | ✅ **폐회로(닫힌 코스)로 확정** — 사용자 확정. 따라서 `_find_segment` 전역탐색 버그는 "잠재 위험"이 아니라 **실제 발현 조건**이다. R2 최우선 항목으로 승격, B5 폐곡선 회귀검증을 게이트로 강제 |
| 1 | R4 (A안 채택 시에만) | MC 구간도 A안으로 통일할 것인가 | 미결 — **아니오(FW만)** 제안 유지. `mc_pos_control`은 `course`와 무관하고, `type_mask=3520`은 yaw를 무시해 2026-07-21 yaw 사고 대응이 무력화된다. **A안이 기각되면 이 결정 자체가 사라진다** |
| 5 | R5 (마감 후) | `d_end_thresh` 기본값 | 미결 — 현재 10 → 종점을 46.8m 지나친다. 통과량 ≈ `57 − thresh`이므로 **오버슈트 0을 원하면 ≈57**. **R3 이후 확정** |

---

## 4-1. ⚠ A안 심사 방침 — 사용자 지시 (2026-07-27, 최우선)

**사용자가 A안을 "매우 부정적으로" 검토하라고 명시 지시했다.** 근거: 도입 이득이 미약한 데 비해
위험 부담이 있고, 2차 예선이 코앞이다. 따라서 §2-1의 채택/기각 기준을 아래로 **덮어쓴다**.

**기본 입장은 「기각」이다.** A안은 "위험이 발견되지 않았으니 채택"이 아니라,
**"아래를 전부 충족시켜야만 비로소 채택"** 이다.

1. **입증 책임이 A안 쪽에 있다.** 리스크 R-a~R-f **6건 전부**에 대해, 소스 줄번호 또는 실측으로
   **"실재하지 않는다" 또는 "코드로 완전히 봉쇄했다"** 를 보여야 한다.
   **하나라도 "아마 괜찮을 것"·"관측되지 않았다"·"조건부"로 남으면 → 즉시 기각.**
2. **이득도 입증 책임이 있다.** §2-1 채택기준 1(바람 8m/s cte 개선)이 **실측으로 유의하게**
   나와야 한다. "이론적으로 더 낫다"는 근거가 되지 않는다 — §0이 지적했듯 A안의 증분 이득은
   **한 번도 측정된 적이 없다.**
3. **의심스러우면 기각한다.** 애매한 결과, 재현 안 되는 개선, 해석이 갈리는 수치는 **전부 기각 사유**다.
4. **마감이 가산점이 아니라 감점이다.** "시간이 남으니 해보자"가 아니라, 제어법칙 교체는
   그 자체로 2차 예선 리스크다. 실기체 FW+OFFBOARD 실적이 **0건**인 상태에서
   유도법칙까지 바꾸면 첫 실비행에 미검증 변수가 두 개가 된다.
5. **기각이 정상 종료다.** 기각 시 조치는 이미 정해져 있다 — 현행 유지 + PX4 핀 문서화 +
   업그레이드 필요 시 상류 한 줄 패치(`course = NAN`). 이건 패배가 아니라 **더 작고 되돌리기 쉬운 해법**이다.

**뒤집히는 유일한 조건:** 위 1·2를 **동시에** 만족 — 6개 리스크 전부 소스/실측으로 봉쇄됨이
증명되고, **그럼에도** 바람에서 유의한 개선이 실측될 때. 이때만 R4로 진행한다.

---

## 5. 검증 게이트 공통 규약 (오케스트레이터용)

- **세션 자기보고를 그대로 믿지 않는다.** 매 세션 종료 후 오케스트레이터가 직접 재현한다 —
  `git log`/`pytest` 재실행은 최소선, 수치 주장은 **원본 로그(node.log/ulog)에서 직접 교차확인**.
  실제로 이번 캠페인에서 요약과 원본이 어긋난 사례가 있었다(C4 "정렬 잔류오차 15.3°" →
  실제로는 정렬 완료 오차 불변, **허용대 재이탈 12.5°**가 진짜 신호였다).
- **"이 테스트가 진짜 버그를 잡는다"는 주장은 코드를 일부러 깨뜨려 red 확인 → 원복 green 재확인.**
- **경고의 무해성은 세션이 아니라 오케스트레이터가 게이트에서 판단한다.**
- **null 결과는 기능부재를 결론내기 전에 측정방법부터 의심.**
- 세션이 idle 루프("백그라운드 대기합니다"만 남기고 턴 종료)에 2회 빠지면 즉시 `TaskStop`하고
  오케스트레이터가 폴링을 넘겨받는다.

---

## 6. 환경 함정 (그대로 이어짐)

- `wsl.exe -d ... -- bash -lc '...'` 는 복잡한 셸 구문이 깨진다 → **로직은 스크립트 파일, 한 줄 호출**
- **PX4 콘솔(`pxh>`) 파일 리다이렉트 금지** — 분 단위 GB급 폭주
- 시나리오 사이 **`wsl.exe --terminate Ubuntu-22.04`** (개별 kill로는 gz 서버가 남아 중복 인스턴스)
- SITL arm: `CBRK_SUPPLY_CHK=894281`, `NAV_DLL_ACT=0` — **실기체 파라미터엔 절대 미적용**
- `fc_bridge`는 `pip install -e .` 금지(`.pth` 방식)
- WSL 클론에서 **`git clean` 금지** — gitignore된 `.ulg` 산출물이 지워진다
- 산출물 이송: WSL 클론 → `/mnt/c/sitl7_xfer/` → 호스트 워크트리. `.ulg`는 WSL 안에만
- 시뮬 정지 판별: `metrics.json`의 `ulog_duration_s / elapsed_s` (정상 0.92~0.99).
  벗어나면 PX4/코드 탓이 아니다 — `_simstall`로 보존 후 재실행
- 하니스 한계: `trajectory_setpoint` ulog 5Hz(발행 10Hz)라 **"계단 없음"을 주장하려면
  `resumption_gaps`까지 볼 것** / 시각정렬 잔차 ±1s 초과 런은 "경계 위반 건수" 신뢰 불가 /
  `setpoint 점프` 임계 1.5m는 FW 순항 lookahead 전진량보다 작아 상시 초과(경계 위반만 볼 것) /
  수직가속은 접지 충격이 지배(`excl_touchdown` 필드)

---

## 7. 회귀 시나리오 필수 요건 (이번 캠페인이 실증한 것)

1. **비-정북 레그를 반드시 포함시킬 것.** 정북 직선만으로는 "정북으로만 나는" 급의 버그가
   5건 연속 통과했다.
2. **런마다 PX4 빌드 커밋을 기록할 것** (`PX4_BUILDS.md`, `meta.json.px4_head`).
   어느 PX4에서 돈 결과인지 모르면 해석이 불가능하다.
3. **완주(PASS)를 성공으로 읽지 말 것.** B5는 4변을 다 돌았지만 종점 마진이 15m로 아슬아슬했고,
   B7은 "완주"했지만 추종 구간이 0.9초였다. **WP별 최근접 거리를 함께 볼 것.**
